---
type: report
packet: LEVEL-1
role: programming
date: 2026-09-04
branch: feat/2026-09-04-level-1-ld-egg-port
base: main @ c16414d
commit: 80a3e46
sources: Sail master 583ecc02 (LD-2/4/6) · PR #375 33ed5eb3 (LD-3) · PR #377 d7510140 (EGG-1) · PR #378 825e1087 (EGG-2)
---

# LEVEL-1 — the LD wave and the easter eggs come over from `Sail`

Both halves of the level are now in the shipping repo: the content Talon last directed
(his 2026-08-30 notes 8, 9 and 10, which became EGG-1 and EGG-2) and the instruments that measure it
(LD-2's cadence readout, LD-3's audit scripts, LD-6's kit and validator, LD-4's wind layer). This is
also **the first time EGG-2 has ever been run against a suite** — it was an open PR in `Sail` that
no marathon had covered.

## What was done

Seven scope items, all carried.

| # | wave | what landed |
|---|---|---|
| 1 | LD-2 | `CadenceClock.cs`, `CadenceTracker.cs`, `PerfReadout`'s since-act/stop-seconds extension, three telemetry payload fields with `TelemetrySelfTest` + `UsageNoticeTests` coverage, `tests/Run-Ld2CadenceCapture.ps1`, `tests/unit/CadenceClockTests.cs` |
| 2 | LD-4 | `SpeedWindCurve.cs`, `SpeedWindLayer.cs` **including `b5dea4dd`'s pause-path fix**, `tests/unit/SpeedWindTests.cs` |
| 3 | LD-6 | `docs/levels/KIT.md`, `tools/dev/kit_course.py`, `kit_selftest.gd`, `kit_course_capture.gd`, `motor_arc.gd`, `tests/Run-KitCourseTest.ps1`, and `scenes/dev/KitCourse.tscn` **regenerated by its own generator, never hand-copied** |
| 4 | LD-3 | both audit documents (bannered), `ld3_cadence.py`, `ld3_cadence_dump.py`, `ld3_capture.ps1`, `ld3_crops.py`, `ld3_tscn.py`, `ld3_weenie_chain.py`, and the `INTAKE.md` §6 edit |
| 5 | EGG-1 | `scenes/game/world/puffinlab/**`, `scripts/game/world/puffinlab/**`, `assets/models/puffinlab/**`, the `Boot.cs`/`LaunchOptions.cs` hunks, `tests/Run-PuffinLabTest.ps1` |
| 6 | EGG-2 | four edited section scenes, `SecretBubble.cs`, `BubbleTestWatcher.cs`, `BubbleCounter.cs`, `TvPortalSelfTest.cs`, `WaterGeometry.cs`, `BubbleTestLayout.cs`, `BubbleTestSelfTest.cs`, `BubbleTestWorld.cs`, the `Gameplay.cs` hunk, `tests/Run-WatcherNightGateTest.ps1`, `tests/Run-EggCapture.ps1` — **minus the branding hunk and the THRILL-BIBLE hunk** |
| 7 | — | three suites registered (appended), both suite commands run on the combined tip, EGG-2's eleven and EGG-2b's four criteria ruled with evidence |

`126` files staged, every one by explicit path. Nothing under `scripts/ui/`, `scenes/ui/`,
`deploy/`, `export_presets.cfg`, `docs/CANON.md`, `CLAUDE.md`, `scripts/dev/`, `scripts/net/` or
`docs/THRILL-BIBLE.md` is touched — asserted mechanically against the staged file list before
committing, not by inspection.

### How each file was carried, and why it matters

The dispatch was explicit that PR #2's work must not be reverted by a file copy. **Every file whose
`Watis` content had diverged from `Sail` was patched as a hunk; only files proved byte-identical at
the blob level were copied whole.** The proof, run before any file was written:

- **Byte-identical to their `Sail` blobs, so copied whole (safe):** all four section `.tscn`s,
  `BubbleTest.tscn`, `BubbleTestLayout.cs`, `BubbleCounter.cs`, and — against `Sail`'s *pre-LD-2*
  tree — `PerfReadout.cs`, `TelemetryPayload.cs`, `TelemetrySelfTest.cs`, `BubbleResetLever.cs`,
  `TvPortalHost.cs`.
- **Diverged, so patched as hunks:** `Telemetry.cs` (carries the extraction's `CampfireMenu` →
  `MainMenu` rename, which a whole-file copy would have reverted), `Boot.cs` and `LaunchOptions.cs`
  (carry the extraction's removal of `--dump-scene`, and are files PR #2 also edits),
  `Gameplay.cs`, `BubbleTestWorld.cs`, `BubbleTestSelfTest.cs`, `WaterGeometry.cs`,
  `TvPortalSelfTest.cs`, `SandboxAvatar.cs`, `UsageNoticeTests.cs`.

`Gameplay.cs` was the only conflict: EGG-2 rewrites the `BuildWorld` water line, and the extraction
had already deleted the comment above it that mentioned the camp. Resolved by hand, keeping the
extraction's deletion and EGG-2's code.

---

## Acceptance criteria — this packet's ten

**1. `dotnet build WatisWorld.sln` — 0 errors.** PASS. Clean on the committed tip.

**2. `Run-BubbleTestWorldTest` on the combined tip.** PASS, and this is the criterion that proves
EGG-2's scene edits are authored rather than code-built. Packed-vs-live counts equal on **all seven**
sections including the four EGG-2 edited ones:

```
[bubbletest-selftest]   Hub            packed[mesh=69 shape=41 body=24] live[mesh=69 shape=41 body=24] OK
[bubbletest-selftest]   RedCairns      packed[mesh=167 shape=164 body=148] live[mesh=167 shape=164 body=148] OK
[bubbletest-selftest]   BluePrecision  packed[mesh=197 shape=116 body=101] live[mesh=197 shape=116 body=101] OK
[bubbletest-selftest]   CyanRun        packed[mesh=114 shape=85 body=52] live[mesh=114 shape=85 body=52] OK
[bubbletest-selftest]   GreenHills     packed[mesh=30 shape=59 body=12] live[mesh=30 shape=59 body=12] OK
[bubbletest-selftest]   Tangle         packed[mesh=197 shape=196 body=184] live[mesh=197 shape=196 body=184] OK
[bubbletest-selftest]   TvRoom         packed[mesh=485 shape=59 body=50] live[mesh=485 shape=59 body=50] OK
```

The collider audit's control fired in the same run:
`collider-audit positive control FAILED AS EXPECTED — control: MeshInstance3D './UncollidedSlab' has
no collision`.

**3. `Run-KitCourseTest` with both positive controls; generator re-run reproduces the scene.** PASS,
after a fix — see Corrections §1. Both controls fired:

```
generator positive control FAILED AS EXPECTED (exit 1)
  FAIL [run-sign]: Wedge_40deg is authored to climb along (1.0, 0.0) but the geometry actually
  written for it climbs along (-1.0000, -0.0000) — dot -1.0000.
self-test positive control FAILED AS EXPECTED (exit 1)
  [kit-selftest] Wedge_40deg is authored to climb along (+1.00, +0.00) but the shipped geometry
  climbs along (-1.0000, -0.0000) -- dot -1.0000.
```

Byte-reproducibility across two generator runs, from the suite's own step 2:
`run A B86FDB1C21EA5F9652FDD6C10866872EDE5943EB2826B8C9A5DA896E2775523E` /
`run B B86FDB1C21EA5F9652FDD6C10866872EDE5943EB2826B8C9A5DA896E2775523E` — equal.

And the criterion's own test, run against the **committed** tree:

```
$ python tools/dev/kit_course.py
[kit] wrote C:\repos\Watis-level1\scenes\dev\KitCourse.tscn
$ git diff --stat
$ git status --porcelain
```

Both empty. The committed scene is exactly what the generator emits. It also matched `Sail`'s
committed `KitCourse.tscn` byte for byte on the very first regeneration in this repo, which is a
stronger result than the criterion asked for: the generator is portable, not just deterministic.

I additionally fired **all twelve** of the generator's declared violations as positive controls.
`12` correctly exited non-zero; `0` wrongly passed. `scenes/dev/KitCourse.tscn` was untouched by
those runs.

**4. `Run-PuffinLabTest`; the seam; the absent case.** PASS on all three.

*The suite:* `PUFFIN LAB TEST OVERALL: PASS`, with both of its own positive controls —
`overlay walker positive control: found 2/2 planted node(s)` and
`clearance positive control: 1 body/bodies inside the floor slab (expected > 0)` — and
`route clearance: 19/19 stations free`, measured against the live avatar's own capsule
(`crown 1.200 m, capsule r 0.145 m h 1.200 m`) including the 1.05 m tiny door.

*The seam, by node path as the packet asked:* both are **direct children of the scene root**, and
the root is `PuffinLab`:

```
PuffinLab/Arrival    [node name="Arrival"  type="Marker3D" parent="."]   Marker3D on solid floor
PuffinLab/ReturnTv   [node name="ReturnTv" type="Node3D"   parent="."]   script = TvPortal.cs
```

confirmed live by the suite:
`seam OK — Arrival at (-7.728, 0.4, 3.312), ReturnTv (MpFoundation.Game.World.TvPortal) at (80.868, -6.9, -6.072)`.

*The absent case, demonstrated rather than asserted.* `PuffinLab.tscn` renamed away, the level
loaded, the log read, the file restored and its hash checked back against EGG-1's blob
(`RESTORED BYTE-EXACT`). Four runs per side:

| | exit | ERROR lines | WARNING lines |
|---|---|---|---|
| `--tvportal-selftest`, lab PRESENT (4 runs) | 0 | **1** | 0 |
| `--tvportal-selftest`, lab ABSENT (4 runs) | 0 | **1** | 1 |

**The ERROR delta caused by the absence is zero.** The one ERROR is
`[avatar N] networked spawn without a Sync child — input will be rejected`, a self-test-harness
diagnostic about the probe body it spawns; it is present identically with the lab and without it.
The literal wording "0 ERROR lines" is not achievable on this harness because that line is emitted
on both sides — reported as a delta rather than trimmed to fit. *(Method note: my first count said
`0` present / `1` absent. That was wrong and I corrected it — PowerShell wraps a native process's
stderr as `<exe> : ERROR:`, so an `^ERROR` anchor silently misses it. This is the trap
`.claude/rules/test-suite.md` warns about, met head-on. Recounted with `(^|: )ERROR: `.)*

The refusal is graceful and named:

```
WARNING: [bubbletest.lab] res://scenes/game/world/puffinlab/PuffinLab.tscn is not in this build -
HiddenTv keeps its own room and the lab throwback is absent. This is the EGG-1-has-not-landed case,
not an error.
[tvportal-selftest]   portals found: 12 (expected 12 — 6 route(s), an entrance and a way back each;
                                          the lab is absent)
[tvportal-selftest]   lab route ABSENT — ... This is a supported state (EGG-1 may land after EGG-2),
                                          not a failure.
[tvportal-selftest] PASS (6 route(s) wired, ...)
```

and with the lab present:
`[tvportal-selftest]   lab route LIVE — HiddenTv -> (82.272, -13.6, 93.312); ReturnTv at (170.868, -20.9, 83.928) -> (61.083, 35.782, -79.216)`.

**5. `Run-WatcherNightGateTest`; absent by day, present by night, with the control.** PASS.

```
[1/3] midnight -- the creature must appear...
      host ready=1  window OPEN=1  sightings=1  state=Watching lines=5
[2/3] noon -- the creature must NOT appear...
```

The suite is *itself* the control structure: the midnight half is the positive control for the noon
absence half, in the same script on the same tree one flag apart, and it fails if either half is
missing. A broken build, a bot that never connected and a correctly-gated day all produce the
identical empty log, which is why the pair exists.

**This criterion is the reason `scripts/game/watcher/**` had to come back** — see Corrections §2.

**6. `python tools/dev/ld3_cadence.py --check`; the re-audit; the diff against LD-3's table.** PASS
on the check (`[ld3-cadence] every waypoint resolves`, exit 0). Table written to
`docs/levels/2026-09-04-LEVEL-1-cadence-reaudit.md`.

**The diff is ZERO, and the packet expected otherwise.** It said "Cyan's numbers are expected to
move; say by how much." **They moved by 0.0 m.** Measured, not assumed: the same tool run against
`583ecc02`'s section scenes in a scratch tree and against this branch produces two 437-line,
36,728-byte outputs with an empty `diff -u`. Mechanism, from the source diff rather than a guess:
`ld3_cadence.py` resolves a fixed table of *named* waypoints, and EGG-2's deleted-line counts per
section are `CyanRun 1`, `GreenHills 1`, `TvRoom 1`, `BluePrecision 8` — only Blue's eight are a
real removal (the tower's flat `Ground` plate, replaced by four aprons and the moat), and that node
was never a waypoint. EGG-2 is additive everywhere a waypoint lives.

The re-audit document says at length what that does and does not license. Briefly: the authored
walking line is unchanged, but **the cost of falling off Blue's tower changed completely and no gap
in metres moved** — a cadence audit measures the distance between things to reach, never the cost of
missing. LD-3's three findings (96.0 m Cyan aisle, 59.9 m west field, 42.8 m green knoll) are all
still exactly true, which is what makes LEVEL-2's job unchanged.

**7. EGG-2's eleven and EGG-2b's four.** Below, in their own section.

**8. Both suite commands, raw counts, every red discriminated.** Below, in its own section.

**9. Headed captures before/after with an index.** PASS. Eight frames under `docs/qa/LEVEL-1/`,
index at `docs/qa/LEVEL-1/INDEX.md`. Same four cameras and two clock phases on both sides; "before"
is this branch with the four EGG-2 section scenes swapped to `583ecc02` (hash-verified going in and
coming out) so a pair isolates the scene edits alone. The index states plainly that the rainbow
bubble and the Watcher appear in **both** sides because they are added from code, not authored into
a section — an artefact of the method, not evidence they predate EGG-2. `TvRoom` has no frame (its
edit is inside the sealed room block) and is given a structural before/after instead.

**10. This report and the DECISION-LOG section.** PASS. `DECISION-LOG.md` §5 lists every skipped
hunk with its reason, five forced divergences, and the one `MVP-SCOPE.md` §3.1 row reversed.
`MVP-SCOPE.md` gains a §5 "Reversals" section so §3 can still be read as history.

---

## EGG-2's eleven acceptance criteria, verbatim, each ruled

Quoted from `docs/agents/roles/programming/inbox/2026-09-02-EGG-2-surprises-in-the-bubble-level.md`
on branch `feat/2026-09-02-egg-2-surprises` @ `825e1087`.

**1.** *"`BubbleTestLayout.TvRoutes` contains a sixth entry whose entrance position is below
`WaterGeometry.WaterY`, expressed via the shipped authority and not as a literal Y. `TvPortalSelfTest`
passes with six routes."*
**PASS.** `BubbleTestLayout.cs:493` — `new(SunkenTvNodeName, SunkenTvPos, "RoomF/RoomTv", …)` is the
sixth row. Derived, not literal: `SunkenTvBaseY = WaterGeometry.SwimLineY − SunkenTvSwimClearanceM −
TvTriggerFloorAboveBaseM` = `(−0.58 − 1.25) − 0.12 − 0.10` = **−2.05**, which is **1.47 m below**
`WaterY` (−0.58). Measured in-engine, not just derived on paper:
`[tvportal-selftest] sunken TV base y=-2.050, trigger -1.950..-0.750, swim line -1.830, waterline -0.580`.
All six entrances enumerated by the suite: `HubTv`, `HiddenTv`, `RedTv`, `BlueTv`, `TangleTv`,
`SunkenTv`. `[tvportal-selftest] PASS (6 route(s) wired, …)`.
*Evidence:* `scripts/game/world/bubbletest/BubbleTestLayout.cs:355-400,493`; marathon log
`World: TV portal (BT-10) PASS`.

**2.** *"The report states the measured swim distance to the underwater TV, the measured drowning
budget from `DrowningClock`, and the resulting margin as numbers."*
**PASS**, and the numbers are carried from EGG-2's own in-engine bake rather than re-derived, since
this packet ported the constants unchanged and the bake asserts them:
- **swim distance 2.593 m** — the bed crosses the submerged contour at r = 13.888 on the bank side;
  the trigger's leading face sits at 11.295. `13.888 − 11.295 = 2.593 m` from the moment
  `DrowningClock` starts.
- **drowning budget `WaterGeometry.DrownAfterSec = 3.0 s`** (`WaterGeometry.cs:215`).
- **reach in that budget: 4.40 m best** (dry, unencumbered), **3.00 m worst** (carrying + Soaked).
- **margin: 1.81 m at best, 0.41 m at worst.** Passable by every loadout, and not by much.
- The risk is the approach side, not the swim: the screen faces south, so coming from the island
  meets the cabinet hull and costs roughly **3.4 m of arc** on top — past even the best reach.
*Evidence:* `scripts/game/world/bubbletest/BubbleTestLayout.cs:365-393` (the derivation and the
annulus numbers it cites from `tools/dev/bubbletest_bake_green.gd`); `WaterGeometry.cs:215`.

**3.** *"The sixth TV's destination is **not** another instance of `TvRoom.tscn`, and the report says
what it is and why. ABSENCE CHECK."*
**PASS.** It is `RoomF`, "the Deep Room" — a silt floor with drifts and stones under a lit water
ceiling, i.e. the lake from the inside. Nothing in the repo instances `TvRoom.tscn` except
`BubbleTest.tscn` instancing it once as the section; `RoomF` is a `Node3D` authored inside that same
section file.
**Positive control for the absence**, from one census script over `TvRoom.tscn`:

| room | descendants | Den furniture found |
|---|---|---|
| `RoomB` | 25 | Couch, CouchHull, LampGlow, PictureFrame0–3 (**7**) |
| `RoomC` | 24 | Couch, CouchHull, PictureFrame0–4 (**7**) |
| `RoomD` | 25 | Couch, CouchHull, PictureFrame0–5 (**8**) |
| `RoomE` | 24 | Couch, CouchHull, LampGlow, PictureFrame0–2 (**6**) |
| `RoomF` | 22 | **NONE** |

The same check that reports NONE for `RoomF` finds 6–8 in each of the four Dens, so it demonstrably
fires when the furniture is there. `RoomF`'s actual children are `Floor`, `Ceiling`, `Wall{N,S,E,W}`,
`Stone0-2`, `SiltDrift0-2`, `RoomFill`, and the `RoomTv` group.
*Why:* the reward for a drowning risk should not be the room the player has already seen five times.

**4.** *"The lab route is wired to `PuffinLab.tscn`'s `Arrival`, and the level loads cleanly with that
scene absent, logging a warning rather than throwing. Demonstrate the absent case explicitly."*
**PASS.** Demonstrated, not asserted — see this packet's criterion 4 above for the full table, the
renamed-and-restored method with hash verification, the WARNING text, and the zero ERROR delta.

**5.** *"A visually distinct rainbow bubble exists, is reachable, and popping it produces its stated
effect. `BubbleCounter`'s total is unchanged by its presence."*
**PARTIAL — the count half PASSES structurally; the pop-effect half is UNVERIFIED here.**
- *Visually distinct:* yes, and visible in the capture —
  `docs/qa/LEVEL-1/after/sunken/EggCap-22s.png` shows one iridescent rainbow sphere among the
  ordinary bubbles.
- *Count unchanged — structural, which is stronger than a before/after pair:*
  `SecretBubble : Area3D` (`SecretBubble.cs:46`), **not** `: Bubble` (`Bubble.cs:28`). The adoption
  pass is `BubbleCounter.Collect`, which is `if (child is Bubble bubble) found.Add(bubble);`. A
  `SecretBubble` can never be collected, so there is **no code path through which it could be
  counted** — not a tuned zero, an impossible one. `AdoptAuthoredBubbles` logs
  `[bubble] adopted N bubble(s)` and that N is the same set with or without it.
- *Reachable, and the pop's effect:* **not verified by this packet.** No suite gates the reveal
  effect, and I did not drive a bot to it. Stated rather than implied.

**6.** *"The Watcher spawns in this level at night and does not spawn during the day, shown by two
captures at two clock phases. ABSENCE CHECK on the day case."*
**PASS on the mechanism; PARTIAL on the literal wording "shown by two captures".**
The gate is proven by `Run-WatcherNightGateTest.ps1`, in both directions, with the midnight half as
the noon half's control: `sightings=1 state=Watching` at midnight, none at noon, and the script fails
if either half is missing. Two captures at two clock phases exist
(`docs/qa/LEVEL-1/{before,after}/night/`) but **a frame is not the evidence here and I will not
present it as such** — the capture script's own header says the creature stands 18–62 m away and
never inside the viewer's cone, so a shot aimed at the bot systematically misses it. The log is the
witness; the frames are the look.

**7.** *"No file under `scripts/game/watcher/**` changes `WatcherBrain`'s state enum or adds a third
state. `git diff` proves it. ABSENCE CHECK."*
**PASS.** Blob-hash comparison of all ten `scripts/game/watcher/*.cs` against `Sail 583ecc02`: **nine
IDENTICAL**, one changed — `Watcher.cs`, and its entire non-comment diff is:

```
+    public const int NetChannel = 14;
-        TransferChannel = NetProfile.WatcherChannel)]     (x4)
+        TransferChannel = NetChannel)]                    (x4)
```

`WatcherBrain.cs` is byte-identical, and its enum is the shipped two states:
`Absent = 0, Watching = 1`.
**Positive control:** a checker that counts the enum's members reports
`WatcherState members = 2: ['Absent', 'Watching'] → PASS` on the real file, and on a scratch copy
with `Approaching = 2` injected it reports
`members = 3 … FAIL: expected exactly [Absent, Watching]` and exits 1. The check can fail.

**8.** *"`/direct` was invoked for the Cyan item, its ledger write happened, and the built dressing
matches what it directed — or, if the gate failed on premise mismatch, that is reported as a finding
with the gate's actual output quoted."*
**UNVERIFIABLE by this packet, and not a failure of it.** `/direct` was invoked by the EGG-2 agent in
`Sail` on 2026-09-02; its ledger and its `THRILL-BIBLE.md` §12 write live there, and **this packet
was told not to carry the THRILL-BIBLE hunk**. The dressing it directed *is* here — Cyan's
`FinishedRecord` (5 old lanes, 5 ticks, 8 skid marks) and `Gallery` (the benches) are in
`CyanRun.tscn`. What cannot be checked from this repository is whether the built dressing matches
what the gate directed, because the direction is not in this repository. That is a consequence of a
deliberate scope decision, recorded in `DECISION-LOG.md` §5.1.

**9.** *"The tower floor is water sourced from `WaterGeometry`, and a player falling from the jumping
section lands in it and drowns — shown by capture."*
**PASS on the source and the geometry; PARTIAL on the drowning, which is shown by construction
rather than by a fall.**
- *Sourced from `WaterGeometry`:* yes, and this is the change that made `ActiveLake` become
  `ActiveWaters`. `Gameplay.BuildWorld` now calls `WaterGeometry.WatersForWorld(id)`, which returns
  `{ BubbleTestLake, BluePrecisionMoat }` for `bubbletest` and `{ CampLake }` for every other id.
  `DepthAt` takes the **deepest** answer across active bodies, so an overlap can never read
  shallower than either.
- *The geometry:* `BluePrecision.tscn`'s single flat `Ground` plate is gone (the only node EGG-2
  removed anywhere) and in its place are four `Ground/Apron*` dry-rim bodies and a `Moat` with a bed,
  four walls and a core footing.
- *Shown by capture:* **yes** — `docs/qa/LEVEL-1/{before,after}/moat/EggCap-22s.png` is an
  unambiguous pair, flat ground before, a dark specular basin after.
- *A player actually falling and drowning:* not driven by this packet. The drowning is by
  construction — the basin is a `WaterGeometry` body, `DepthAt` covers it, and `WaterGeometry.cs`'s
  own note records that the basin is deliberately vertical-walled with `JumpAllowed` false while
  Swimming, so there is no climbing out. Not a frame of a death, and I am not claiming one.

**10.** *"`Branding.Wordmark` is `Watis World` and the splash shows `Great-Grand-Software`; no other
string was renamed. ABSENCE CHECK on the second half."*
**UNVERIFIABLE — deliberately, and this is the packet working, not failing.** The branding hunk
(`scripts/ui/Branding.cs`, `Splash.cs`, and that file's half of `tests/unit/UsageNoticeTests.cs`) is
**named out of scope** by the LEVEL-1 dispatch: `scripts/ui/**` belongs to the UI wave on PR #2. The
hunk was not carried, so `Branding.Wordmark` is untouched on this branch.
The **second half nonetheless holds and is checkable**: no string was renamed by this branch. The
only edit this branch makes to `UsageNoticeTests.cs` is LD-2's eleven-line field-coverage hunk —
which is a *different, non-overlapping* hunk in the same file, and belongs to this packet's scope
item 1 ("the telemetry payload fields and their tests"). EGG-2's branding hunk in that file starts at
line 525 and is absent; verified by diffing the branch's change to that file against EGG-2's.
**Whoever lands PR #2 or a follow-up owes criterion 10 its first half.**

**11.** *"Both suite commands run in the FOREGROUND, unfiltered, before the final commit, with RAW
counts from each quoted — not a verdict. Any red the base `583ecc02` passes: STOP and report exactly
what failed."*
**PASS.** See the suite section below. Both commands, raw counts, every red discriminated against
this repo's base `c16414d` (which is the right base here — `583ecc02` is `Sail`'s and is not an
ancestor of anything in this repository).

## EGG-2b's four acceptance criteria

Quoted from `docs/agents/roles/programming/inbox/2026-09-03-EGG-2b-suite-evidence-and-integration.md`.

**1.** *"Raw counts from both commands appear in the report … each attributed to the command that
produced it. No bare total is written into `.claude/rules/test-suite.md`."*
**PASS.** Both counts below, each attributed. **`.claude/rules/test-suite.md` is not edited by this
branch at all** — MOVE-1 is editing it concurrently and nothing measured here is new to its entries.

**2.** *"Every red in the marathon is individually ruled flake / load / pre-existing / reachable, with
the measurement that decided it. … Absence of reds is also acceptable — say so with the counts."*
**PASS.** Below.

**3.** *"`git status --porcelain` on the branch shows nothing outstanding except generated `.import`
and `.uid` churn, and the branch is pushed with a PR open."*
**PASS.** `git status --porcelain` is **empty** after the commit and again after re-running the kit
generator. No `.import`/`.uid` churn at all — this repo `.gitignore`s `.godot/`.

**4.** *"ABSENCE CHECK — the verifier owes a positive control. No file from commit `9f638b9c` is
modified by this packet's commits."*
**NOT APPLICABLE, stated rather than skipped.** `9f638b9c` is a commit in `Sail`, on the EGG-2
branch; it has no counterpart in this repository's history and `git diff 9f638b9c..HEAD` cannot be
formed here. The equivalent this packet CAN answer — "only paths under 'What you own' are present" —
is answered above and mechanically: the staged file list was grepped for every forbidden prefix
before committing and came back clean.

---

## The suites

Both commands, foreground, on the committed tip `80a3e46`. Logs written **outside** `tests/logs/`.

### `powershell -File tests/Run-AllTests.ps1 -MutexTimeoutMinutes 180`

Counted from the run's own `=== summary ===` block with the anchored pattern `^  .+ (PASS|FAIL)$`.

```
OVERALL: FAIL
```

**RAW: 42 PASS, 1 FAIL, of 43 registered suites.** 43 registered, 43 started, 43 in the summary —
reconciled, so nothing was silently skipped. The one FAIL is `Bubbles: shared counter`, which is
the pre-existing red this packet was told to confirm and set aside.

All three newly registered suites are green: `Level: the kit (LD-6) PASS`,
`Egg: puffin lab (EGG-1) PASS`, `Watcher: night gate PASS`.

This is the **second** marathon on this branch. The first ran on the pre-fix tree and is reported
below rather than discarded, because the difference between the two is the evidence.

### `dotnet test tests/unit/SailNet.Tests.csproj`

```
Passed!  - Failed:     0, Passed:  1716, Skipped:     0, Total:  1716, Duration: 3 s - SailNet.Tests.dll (net8.0)
```

**RAW: 1716 passed, 0 failed, 0 skipped, of 1716.** This run found a real regression first — see
Corrections §3 — and this is the count after the fix.

### Discrimination

**Marathon 2 (the committed tip, `80a3e46`) — one red, and it is the named one.**

| suite | ruling | the measurement that decided it |
|---|---|---|
| `Bubbles: shared counter` | **PRE-EXISTING** | Exactly the failure the dispatch described and `DECISION-LOG.md` already records as a knife-edge window (`$LateDurationSec = 26`). Confirmed once on my own base as instructed, not chased. |

Its raw failure, verbatim, so it can be compared next time rather than re-litigated:

```
lever: accepted=1 refused=1 resets broadcast=1
lever: warnings armed=2
lever: stray pulls armed=2 accepted=0 tally 2 -> 2
A (walker)   samples=176  worst bob offset=0.1356 m
B (witness)  samples=176  worst bob offset=0.1356 m
C (late)     samples=128  worst bob offset=0.1350 m
BUBBLE SYNC TEST FAILED (1 failure(s)):
  - C (late) never saw the reset return the tally to 0 and every bubble to visible
```

Note the **quantities are healthy**: the lever bookkeeping is exact, and the three peers' worst bob
offsets agree to 0.6 mm across 480 samples. Only the late joiner's reset observation is missed,
which is the timing window, not a replication defect. **Provenance:** the P3 session measured this
failing 3/3 standalone at clean `Sail` HEAD `467bbeb` with all in-flight work stashed out, on an
idle machine, 2026-09-04. It has its own packet. Not chased, not allowed to gate this PR.

**Marathon 1 (the pre-fix tree) — five reds, all four non-mine ruled, the fifth fixed.**

| suite | ruling | the measurement that decided it |
|---|---|---|
| `Level: the kit (LD-6)` | **REACHABLE — mine, root-caused, FIXED** | `SCRIPT ERROR: Parse Error: Preload file "res://tools/dev/motor_arc.gd" does not exist.` A missing ported file, not a flake. Fixed by porting it; the suite passes in marathon 2 and passed standalone in between. See Corrections §1. |
| `Security: hardening` | **LOAD** | Discriminated properly, because unlike the other two this suite passes no `--world` and so runs on `DefaultWorld = "bubbletest"` — my diff genuinely reaches it. Run 3x standalone per side: **base `c16414d` 1 red / 3**, **branch 1 red / 3**. The two sides even failed at *different* steps (base: `server 'srv-version' never came up`; branch: `expected exactly 6 clients to join, got 5`), which is load's signature — a regression reproduces the same failure. On every green run, both sides, the raw quantity is identical: `ok (6 joined, 1 refused)`. Green in marathon 2. |
| `Carry: regrab-while-loose` | **LOAD** | Named load-sensitive in `.claude/rules/test-suite.md`, and its red is the exact shape that rule predicts: *"its reds are STAGING failures — read the failing check's NAME"*. The name was `bot A never completed held -> loose -> held-again for prop 2 (reached phase 2) - regrab staging broken`. Staging, so the rule's quantity (worst hold distance) was never reached to be compared. **Also provably unreachable:** it runs `--world propsync`, and for any non-`bubbletest` id `WatersForWorld` returns `{ CampLake }` — bit-identical to the `ActiveLake = LakeForWorld(id)` it replaced. Green in marathon 2. |
| `World: run driver` | **LOAD** | Its red is literally the shape the rule names for its sibling `World: tidal-cycle phase` — *"a two-client phase comparison with a 1.5 s tolerance — compare the divergence"*: `post-reset phase disagreement between RunEarly (0.00011574074) and RunLate (0.27370372) - 2.7358797926s apart (tolerance 1.5s)`. A clock-divergence measurement under load, and its own ordinal-math self-test passed in the same run. **Also provably unreachable:** runs `--world open`, same `WatersForWorld` argument. Green in marathon 2. |
| `Bubbles: shared counter` | **PRE-EXISTING** | As above. Red in both marathons, identically. |

**The reachability argument, stated once because three rulings lean on it.** Only three things in
this diff can execute in a world other than `bubbletest`:

1. `Gameplay.BuildWorld`'s `WaterGeometry.ActiveWaters = WatersForWorld(id)`. For any id that is not
   `"bubbletest"`, `WatersForWorld` returns `{ CampLake }`; the `ActiveLake` setter it replaced wrapped
   `LakeForWorld(id)` — also `CampLake` — into a one-element array. **Bit-identical.**
2. `CadenceTracker`, reached only from `Telemetry.BeginClientSession`, which `Boot.cs:346` calls only
   on the Splash path and only `if (!net.IsHeadless)`. **Every scene-suite peer is headless**, so the
   tracker is never constructed and every `NoteAct` call site is a null-guarded no-op.
3. `SandboxAvatar`'s two new members — `IsLocallyDriven` and `MotionNow` — are read-only accessors
   with no call site outside `CadenceTracker`, which per (2) does not exist in a suite.

The Watcher, the lab, the secret bubble and the moat are all constructed by `BubbleTestWorld` and
cannot reach any other world.

**No STOP condition fired.** The stop condition was "any suite red other than `Run-BubbleSyncTest`
that main passes and your diff reaches". `Level: the kit (LD-6)` was reachable but is a *new* suite
main does not have, and it was root-caused and fixed rather than re-run. Everything else red was
either load (green on both sides, identical raw quantities) or the named pre-existing one.

**No mutex timeout occurred.** Both marathons printed an `OVERALL:` line and non-empty summaries, so
neither was a starved run. MOVE-1 held the machine when this packet began; the port was done first
and the mutex taken only when measuring, exactly as dispatched, and 2–6 sibling Godot processes were
observed during the first marathon and the Security discrimination (recorded because it is the load
the ruling above rests on). No process I did not start was touched.

---

## Corrections to the packet

**§1 — LD-6's kit self-test has a seventh file the packet's list did not name, and without it the
suite is red.** `tools/dev/kit_selftest.gd:44` opens
`const MOTOR := preload("res://tools/dev/motor_arc.gd")`. `motor_arc.gd` is not in the packet's LD-6
file list and is not in `caac71a4`'s diff (it predates the kit). Ported from `583ecc02`. **Found by
the suite, not by reading** — the first marathon's failure was
`SCRIPT ERROR: Parse Error: Preload file "res://tools/dev/motor_arc.gd" does not exist.` This is the
argument for registering a ported suite and running it rather than trusting a file list.

**§2 — EGG-2's night gate cannot be met without reversing an `MVP-SCOPE.md` §3.1 cut, and the packet
did not say so.** The packet's acceptance criterion 5 requires `Run-WatcherNightGateTest` to pass,
and EGG-2's own criterion 7 is an absence check *over `scripts/game/watcher/**`* — a path the
2026-09-02 extraction had removed entirely as "the threats of the previous premises". The build
failed with six errors naming `Watcher`, `WatcherState`, `INightPressure` and
`MpFoundation.Game.Contracts`. Brought back verbatim: `scripts/game/watcher/**` (11 files) plus
`INightPressure.cs` and `IVisibilityScore.cs`. **The fauna and the eel kit stay cut.** Recorded in
`DECISION-LOG.md` §5.3 and `MVP-SCOPE.md` §5.1, including the consequence the packet also did not
state: in this world the creature is **not** flag-gated, so playing at night now means a creature may
appear.

**§3 — the packet's "port EGG-2 without its `UsageNoticeTests.cs` hunk" is right, but that file has
TWO hunks and LD-2 owns the other one.** Skipping the whole file made
`UsageNoticeTests.TheNoticeCopy_CoversEveryFieldTheUsageReportSends` fail:
`TelemetryPayload sends field(s) the usage notice was never checked against: stop_seconds,
stop_seconds_by_section, stop_seconds_per_minute`. That guard exists precisely to catch a telemetry
field added without checking the disclosure copy still tells the truth, and LD-2's own eleven-line
hunk answers it. Applied; EGG-2's branding hunk (line 525+) still excluded. Scope item 1 says "the
telemetry payload fields **and their tests**", so this is inside the packet, but the exclusion
sentence reads as covering the file.

**§4 — three named files could not be written where `Sail` has them, and were relocated in scope
rather than written out of it.** Each is in `DECISION-LOG.md` §5.2 with its full reasoning:
`PauseOverlay.IsOpen` → a cached node lookup inside `CadenceTracker` (`scripts/ui/**` is PR #2's);
`NetProfile.WatcherChannel` → `Watcher.NetChannel`, same value 14 (`scripts/net/**` is not this
packet's, and `NetProfile`'s ladder already records 14 as a cut system's and names 17 as next free);
`assets/models/objects/Box.glb` → `assets/models/puffinlab/Box.glb` (the extraction cut it, EGG-1's
`LabRoom.tscn` instances it three times, and only `assets/models/puffinlab/**` is in scope).

**§5 — the packet's expectation about the cadence re-audit was wrong, and the measurement says so.**
"Cyan's numbers are expected to move" — they moved by 0.0 m, for a reason the re-audit document
gives mechanically. Reported as a finding rather than massaged into a delta.

**§6 — LD-4's wind layer is here but nothing instantiates it.** `SpeedWindCurve`, `SpeedWindLayer`
and `SpeedWindTests` are ported and green, but LD-4's only wiring is in
`scenes/dev/MovementPlayground.tscn`, which is MOVE-1's file by name. So the code is present and
inert in this repo until someone wires it. Named here as a live loose end rather than left to be
discovered.

---

## The bible check

```
Bibles applied:  Level (composition — the ported sections and the kit course), Interaction (the
                 sixth television and the secret bubble), Mechanics (the Watcher's night gate, the
                 drowning moat, the counter). Behavior applies to the Watcher and is checked
                 through Mechanics §2 rather than separately, because this packet changes none of
                 its behaviour — nine of its ten files are byte-identical to Sail. Thrill is NOT
                 applied and cannot be: its bible section is the hunk the packet told me not to
                 carry (EGG-2 criterion 8, ruled UNVERIFIABLE above).

Items checked:   LEVEL-BIBLE §1 The Level Manifest — the ported sections touch `zones[]` (Blue's
                 gradient changes: a consequence-free floor becomes a lethal one), `path_topology`
                 (the sixth TV adds a route to the hub-and-spoke set, taking it from five spokes to
                 six), `return` §6 (the sunken route is the one entrance a player cannot be
                 returned TO — `SunkenTvReturn` is a named dry-bank return at r=18 rather than
                 `RoomReturnOffset`, which would land them 0.47 m under the waterline with a fresh
                 drowning clock), and `threats[]` §7 (the Watcher instantiates here, at night, as
                 this level's only threat — §7.1's independence law is trivially satisfied because
                 there is nothing for it to interact with).
                 INTERACTION-BIBLE §4 Range & Timing (the sixth TV's trigger straddles the swim
                 line by construction: box −1.95..−0.75 with a swimmer's feet at −1.83 inside it);
                 §5 Multiplayer Contention (the TV portal's 800 ms re-entry gate, asserted live:
                 `cooldown probe: re-entered after 250 ms (gate is 800 ms); teleported=False`);
                 §6 Reversibility (the secret bubble is one-time and resets on the counter's reset
                 signal, riding `BubbleCounter.Changed` rather than adding a second RPC).
                 MECHANICS-BIBLE §2 State Transition Integrity and §4 Idempotency for the Watcher
                 gate — the creature is server-authoritative with WatcherNetState's sighting-id
                 guards, so an ApplyGone cannot overtake its own ApplySighting; §1 Boundary
                 Conditions for the moat (DepthAt takes the deepest of the active bodies, so an
                 overlap can never read shallower than either).

Result:          Pass, with one item explicitly not closed and one not applicable.
                 NOT CLOSED: LEVEL-BIBLE §1's manifest is not filled in for this level — it never
                 has been, this packet did not create that gap, and filling it is a level-design
                 act that belongs to LEVEL-2 rather than to a port. Named rather than skipped.
                 NOT APPLICABLE: THRILL-BIBLE, for the reason above.
                 Nothing had to be fixed to make the applicable items pass: the ported code already
                 satisfied them, which is the expected result for a port whose job is fidelity.
```

---

## Open questions, ripe

**1. The kit course is not rideable, and nobody owns making it so.** Talon, today: *"Please simply
include the bike and bike movement in the level which is being tested. Because I need to roll down
slopes and things like this."* MOVE-1 puts the bike in this repo; this packet puts the level in it;
**neither builds the combination**, and MOVE-1's packet excludes `scenes/dev/KitCourse.tscn` by name
as mine while mine says port the kit exactly as LD-6 built it.

Having now read both the scene and its generator, here is the smallest honest change, and it is
**not** a course-content change. `KitCourse.tscn` is emitted by `tools/dev/kit_course.py`, whose
`ext_resources` are exactly four: `SandboxWorld.cs`, `SandboxCamera.cs`, `SandboxAvatar.cs` and the
checker shader. The course itself is 37 pieces of static geometry on one ground plate with a single
`spawn` marker and a catch floor at −6 m — geometry a bike can already roll down, because the
generator validates every surface against `MotorTuning.Default`'s own derived arc and the pieces are
plain `StaticBody3D` colliders with no avatar assumptions in them. **What is missing is a rider, not
a ramp.** The bike in MOVE-1 is a playground layer (`scripts/dev/playground/BikeLayer.cs`,
`BikeRig.cs`, `BikeCamera.cs`) that `MovementPlayground.tscn` composes; `KitCourse.tscn` composes
`SandboxWorld` + `SandboxCamera` + `SandboxAvatar` and knows nothing about it.

So: **it is a spawn-side change, and the file that changes is `tools/dev/kit_course.py`** — never
`KitCourse.tscn`, which is generated and would be overwritten by the next run. Concretely, the
generator would emit a fifth `ext_resource` for the bike layer and one node beside the existing
`spawn` marker, exactly the way it already emits the camera and the avatar. That is a handful of
lines in `emit()`. **Whether the bike can be composed into a non-playground scene at all is the part
I cannot answer from here** — MOVE-1's tree is uncommitted and I did not read its internals, and if
`BikeLayer` depends on `MovementPlayground` rather than on a bare `SandboxWorld`, the smallest change
becomes "give the bike a scene-agnostic entry point", which is `scripts/dev/**` and squarely MOVE-1's.
**Owner: MOVE-1 for the bike-side question, whoever holds `tools/dev/**` for the generator line.**
Not built, not prototyped. Talon decides.

**2. LD-4's wind layer has no host in this repo.** See Corrections §6. Should `SpeedWindLayer` be
wired into the bubble test (where a player actually is), or wait for `MovementPlayground.tscn`?
That is a direction call: the wind is a speedometer, and whether the shipping level wants a
speedometer is Talon's, not mine.

**3. The night creature now appears in the shipping level with no flag.** Recorded as a fact rather
than a complaint (`DECISION-LOG.md` §5.3) — it is exactly what note 9 asked for. But it is the first
time this repo has had a threat since the extraction stripped them, and the next person to run a
playtest at midnight should not be surprised by it.

## Not done, and why

- **EGG-2 criterion 10's first half** — the branding rename. Out of scope by dispatch; PR #2's.
- **EGG-2 criterion 8** — `/direct`'s ledger. Unverifiable without the THRILL-BIBLE hunk, out of
  scope by dispatch.
- **The secret bubble's pop effect** and **an actual fall-and-drown in the moat** — both are by
  construction here, not driven. No suite gates either.
- **LD-3's findings** (96 m Cyan aisle, stepping-stone gaps, the missing weenie link) — LEVEL-2.
- **EGG addendum §8**, the unidentified level piece — still Talon's, not guessed at.
- **LD-1's metrics gym, LD-5's Jolt switch, the LEVEL-BIBLE blanks, any new geometry, any UI.**
- **`Run-BubbleSyncTest`** — pre-existing, its own packet.

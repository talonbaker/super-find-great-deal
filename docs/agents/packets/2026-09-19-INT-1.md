# PACKET INT-1 — the final merge, the full gate, and the ride card

Program: `C:/repos/PROGRAM-2026-09-19-SUPERMARKET.md` §3 (INT-1 row), §8 (definition of done).
Orchestrator: FABLE_SUPERMARKET. Role: integration (Opus). PR: none. Base: `integration/2026-09-19-mvp`
@ `0e75b0ae1d45c479ffa65085ab1e0005e1e1135a` (INT-0B's tip) in `C:/repos/sfgd-int`. Read
`docs/agents/handoffs/2026-09-19-INT-0B.md` first (its merge method and every seam it already
settled), then `2026-09-19-INT-0.md`, then each lane's handoff as you merge it.

## Why this packet exists

Talon's definition of done (§8): two windowed clients from his launcher, direct connect over ENet;
host and joiner in the holding room with names and scores on the board; the hider takes a rack
object and presses Start; 30 s to hide; Confirm; the seeker searches the shelves and bins and drops
the right object in the bin; the door bursts on the hider mid-sort; both press End; the tally; roles
swap; round two; the match card. One directory, one command, a ride card with captures. Then stop.

## Merge, in this order (each its own merge commit; keep-both unless a ruling says otherwise)

Actual merge sequence: **SFX-2 → TASK-1 → HOLD-1** (three merges; BTN-1 and SHELF-1 ride inside HOLD-1).

| Lane | Branch @ tip | Known seams |
|---|---|---|
| SFX-2 | `feat/2026-09-19-sfx-2` @ `2dcd6dd` (off SFX-1 `c3cb295`) | `PropImpact` on **channel 22**; `Release` byte, `ApplyPropState` 6 args; `PropManager.Instance` copied verbatim from DOOR-1 (collapse); `NetworkedProp.Unbind` no longer fires `Dropped`; withdraws SFX-1's `Voice: proximity gate` flake entry — **keep INT-0B's paragraph**, drop SFX-2's duplicate wording; two rules-file entries (event-order assertions; `Run-RoundLoopSmoke` self-mutex) keep |
| BTN-1 | **already inside HOLD-1's branch** (`76377f3` merged at `cb3ac00`); listed for its seams only | buttons inline per room; `RoundControls`, `PressRefusal`; rack sets `TargetPropId`; `--reach-target` pins; `Sfx.Buzzer = 43`; port 7906; placeholder `DealCan/DealBox/DealProduce.tscn` — **SHELF-1's files win**; `Run-PlaceTest` prop-id constants 1003..1006 — **SHELF-1 renumbers again; take SHELF-1's numbers and re-run the suite**; `HoldingRoom.tscn` rack + START transforms (HOLD-1 builds around them) |
| TASK-1 | `feat/2026-09-19-task-1` @ `741d8bb` (off INT-0B `9c16181`) | `TowersCompleted` → `SortsCompleted` / `TowersAtFound` → `SortsAtFound` / script verb `sorts:N` across 20 files (BTN-1 and SFX-2 do not reference it; confirm with grep); `SortBin.tscn`/`SupplyCrate.tscn` in `SectionScenes` (sortables deliberately not — `Carryable` exception); port 7907; `Sfx.SortGood = 46, SortBad = 47` (43–45 gap pinned by a test — BTN-1's `Buzzer = 43` fills it; amend the gap test); its ladder says next free 7908 but SHELF-1 took 7908 — reconcile to one table; `--round-script` `found` is a latching level, a second round needs `lost@N` |
| SHELF-1 | **already inside HOLD-1's branch** (`659f3ee` is its base); listed for its seams only | aisles, bins, real `Deal*.tscn`; authored prop renumbering; `Run-AuthoredPropTest` port 7908; `SearchPillar` status (REACH-1's phase 2 needs an interior static block); `SectionScenes` additions; frame-time and voice numbers |
| HOLD-1 | `feat/2026-09-19-hold-1` @ `ea2fb21` — **contains SHELF-1 `659f3ee` and the verified BTN-1 merge `cb3ac00` + seam fix `d80e061`** (so BTN-1 and SHELF-1 arrive through this one merge; do not merge them separately) | board reads the CARD; practice corner; 144 authored props ids 1000..1143 (all suite constants re-derived on this branch — trust HOLD-1's numbers); `Sfx` 24–43 filled, 44 next free on this branch vs TASK-1's 46–47 with a 43–45 gap test — amend the gap test; port 7909; `Run-HoldingBoardTest` |

Rulings:
1. **`Sfx` enum**: one list 24 → 47 (or the highest taken), every name once; header "Append at N".
2. **`ProtocolVersion` stays 16**; its comment gains SFX-2's `Release` byte and channel 22 as a
   third reason under the same bump. No lane may have bumped to 17 — if one did, fold it back to
   16 with the reasons merged, and say so.
3. **`.claude/rules/test-suite.md`**: every dated section kept, lane order; ladder one table
   7893–7908 (7905 = INT-0B's unregistered probe, note it); the `Voice: proximity gate` paragraph is
   INT-0B's version.
4. **`SupermarketWorldSelfTest.SectionScenes`**: the union of every lane's additions.
5. **Authored prop ids**: SHELF-1's numbering is final; every suite constant that names an authored
   id (BTN-1's `Run-PlaceTest` bump, REACH-1's phases, TASK-1's smoke) is checked against
   SHELF-1's adoption log line and fixed in its own commit if it moved.
6. **Spawn pin for bots (SHELF-1 §C)**: add `--spawn-index <n>` (server-side per peer name or a
   client flag the server honours) so a scripted bot lands on a named marker regardless of join
   order; use it in every suite that walks the search room; the room's four-corridor layout is a
   level change deferred to Talon's ride. One commit, xUnit on the mapping.
7. **Voice budget with SFX-2 merged**: re-run SHELF-1's shove measurement (`--cost-shove-every`)
   and quote `PeakLive3DVoices`/`OneShotSteals` before and after; if steals stay above 50 %, lower
   SFX-2's `MaxImpactsPerTick` from 4 to 2 in its own commit and re-measure — report both.
8. **Two hunks that cannot both be true**: stop, report NEEDS_CONTEXT with both quoted.

## The full gate (this is the one place the marathon runs)

- `dotnet build`: 0 errors; xUnit raw count with the arithmetic against 1560 + each lane's additions.
- `tests/Run-AllTests.ps1` in a **desktop session** (FP-1's suite opens a window): every suite,
  raw `=== summary ===` block quoted. Any red: discriminate as the rules file says (standalone ×3,
  quote the lines, check other lanes' handoffs for an incident at that clock minute). A red that is
  a merge defect is yours to fix in its own commit and re-run. The marathon is run once more after
  any fix, whole.
- **The definition-of-done walk, by bots**: one `--round-script` session with two windowed
  `--first-person-cam` bots and `--log-clock --log-sfx`, two full rounds, asserting from the logs
  the §8 sequence: board rows on both peers, rack pickup, Start accepted, Hiding 30 s with clock
  ticks, Confirm accepted (reachable), seeker delivers the target → Found → burst on both peers,
  sort count frozen at the Found tick, End → Tally, swap, round two, the match card with a winner,
  scores zero on the third Start. Captures at each beat from the hider's and the seeker's eye into
  `docs/qa/2026-09-19-int-1/`.
- **Merge to `main`**: `git merge --no-ff integration/2026-09-19-mvp` onto `main` (currently the
  seed commit `34cc3e2`), push `main`. This is the one push to a shared branch in the program; it is
  Talon's standing instruction for INT-1 (program §3).

## The ride card — `C:/repos/RIDE-CARD-2026-09-19-SUPERMARKET.md` (root, not in the repo)

One directory, one command block Talon runs himself (server + two windowed clients on this
machine, or host + joiner; include the Direct Connect address), then: the loop in ten lines; the
knobs he can move without a rebuild (`--startle-file` JSON: `TellSec`, `KickDegrees`, `BangFlatDb`,
`ForceDropOnBurst`; `--intercom-wet-db`; `CarryAnchorRestLocal` if it is file-backed, else say it
is a rebuild); the four things only a hand can judge (BTN-1's reach/travel/shake/targeting; the
crate distance from the eye; the tell delay; the forced drop as payoff or punishment); the captures
inline by path; the raw counts; every open item from every handoff in one table (owner, one line).
Then STOP: no more packets after INT-1 until Talon rides.

Commit by path; never `git add -A`; attribution lines. **Stop Godot only by PIDs you recorded
(`Stop-Proc`); never `taskkill /IM`.** No other lane runs Godot during INT-1's marathon — the
orchestrator guarantees the machine is yours; if you see a Godot you did not start, stop and report.
Never touch any `sfgd-*` worktree but `sfgd-int`.

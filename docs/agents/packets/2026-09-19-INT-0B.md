# PACKET INT-0B — the wave-2 integration base (six landed lanes onto `integration/2026-09-19-mvp`)

Orchestrator: FABLE_SUPERMARKET. Role: integration (Opus). PR: none. Base: `integration/2026-09-19-mvp`
@ `40d55e09e13b9479399dda98f55380da426a2143` in the worktree `C:/repos/sfgd-int` (INT-0's; it is
idle and yours now). Read `docs/agents/handoffs/2026-09-19-INT-0.md` first (how INT-0 verified its
merges: every touched file diffed against BOTH parents, every apparently-removed line accounted for
— do the same), then each lane's handoff as you merge it.

## Why this packet exists

Six lanes landed off `40d55e0` in parallel. TASK-1, SHELF-1 and HOLD-1 each need three or four of
them at once (the burst, the material tag, the reachability source, the match text, the clock
panel). One tree that holds all six is cheaper than three lanes each re-deriving the same merges,
and it takes half of INT-1's merge risk off the final gate.

## Merge, in this order (each its own merge commit, keep-both unless stated)

| Lane | Branch @ tip | Known seams |
|---|---|---|
| VOICE-1 | `feat/2026-09-19-voice-1` @ `c9f39d5` | `RoundStripWidget` third row; `HideSeekDriver.RoomOf`; `--voice-pa-all` sets both hooks |
| DOOR-1 | `feat/2026-09-19-door-1` @ `b8dd329` | `Sfx.Bang = 24`; `SfxLab.PlayUi(bus)`; `PropRegistry.Wake`; `PropManager.Instance`; `FirstPersonCamera.Local`; port 7900 |
| SFX-1 | `feat/2026-09-19-sfx-1` @ `c3cb295` | `Sfx` 25–35; `ActorEvent.Placed = 14`; `PropKind` 2–4; **`ProtocolVersion` 15→16**; `PoolSize` 18 / `LoopPoolSize` 1; port 7902; its rules-file flake entry for `Voice: proximity gate` is WRONG (an external kill — SFX-2 withdraws it on its branch; if SFX-2 has not landed when you get there, withdraw it yourself and say so) |
| REACH-1 | `feat/2026-09-19-reach-1` @ `475cd8a` | `PropRegistry.SetLoose`; kill plane → `LastGoodTransform`; `IConfirmTimeAudit`; world id `reachplant`; port 7903 |
| MATCH-1 | `feat/2026-09-19-match-1` @ `d62a34b` | `HideSeekWireTally` +5 fields; `HideSeekText.StripLine(view, nameOf, tuning)` now chooses the sentence; `BotHarness.RoundTallySample`; did NOT bump `ProtocolVersion` |
| CLOCK-1 | `feat/2026-09-19-clock-1` @ `f5470e0` | `Sfx` 36–42 (`RoundBuzz`, `ResetWhoosh` — keep both names; do not collapse onto `Buzz`/`Whoosh`); `RoundClock.tscn` in `SectionScenes`; `SupermarketWorldSelfTest.CountNodes` stops at instance boundaries; `GameHud.PollIntervalSec` public; `UiNoBespokeStylingTests.AlsoScanned`; port 7904 |

Rulings for the seams the merge cannot decide alone:
1. **`Sfx` enum**: one list, 24 → 42, every name once, ordinals exactly as each lane took them; the
   two reservation comments go away. `SfxLab`'s switch/`Render` gets every recipe.
2. **`ProtocolVersion`**: exactly one bump, 15 → 16, its comment carrying BOTH reasons (SFX-1's new
   `PropKind`s; MATCH-1's five tally fields). Say in the handoff that SFX-2 (still on 16) and BTN-1
   need no further bump.
3. **The clock's tally/Together line** must be MATCH-1's sentence: route `RoundClock`'s `ClockLine`
   through `HideSeekText.StripLine(view, nameOf, tuning)` (or a `ClockLine` overload that calls the
   same chooser) so the clock, the strip and HOLD-1's board cannot disagree. CLOCK-1's `"3 SORTED"`
   copy stays.
4. **`.claude/rules/test-suite.md`**: every lane's dated section kept, lane order; the ladder table
   becomes one table 7893–7904; SFX-1's false flake entry withdrawn (above).
5. **`.claude/rules/godot-scenes.md`**: add CLOCK-1's two lines — `[Export]` on a nested instance
   line is silently dropped on this build (derive identity from the node name); every LEVEL prefab
   must be listed in `SupermarketWorldSelfTest.SectionScenes` or the packed-vs-live check cannot see
   it. Cite CLOCK-1's handoff.
6. `PropRegistry.Wake` (DOOR-1) and `SetLoose` (REACH-1) are different verbs; keep both.
7. Anything else: keep both sides; where both sides cannot both be true, stop and report
   NEEDS_CONTEXT with the two hunks.

## Verify (not the marathon — that is INT-1's)

- `dotnet build`; xUnit raw count (expect 1317 + 25 + 38 + 50 + 17 + 40 + 63 = **1550** if every
  lane's tests are disjoint; report the real number and account for any difference).
- Standalone, `-SkipBuild`, under the mutex, one at a time, quote each OVERALL line:
  `Run-VoiceRoomTest`, `Run-BurstDoorTest`, `Run-MaterialSfxTest`, `Run-ReachTest`,
  `Run-RoundLoopSmoke` (MATCH-1's two-round script), `Run-RoundClockTest`,
  `Run-SupermarketWorldTest`, `Run-MultiplayerTest -BotCount 2`, `Run-FirstPersonTest` (needs a
  desktop session). Any red: discriminate as the rules file says; a red that is a merge defect is
  yours to fix in its own commit.
- Cross-lane check the merge cannot see: DOOR-1's bang and CLOCK-1's cues on the same peer at the
  Found tick (bang from the door, nothing from the clock) — one `--round-script` run with
  `--log-clock`, quote the cue lines around Found.
- Push `integration/2026-09-19-mvp`. Handoff `docs/agents/handoffs/2026-09-19-INT-0B.md`: merge
  SHAs, every conflict and how it was kept, raw counts, the two rules-file additions.

Commit by path; never `git add -A`; attribution lines. **Stop Godot only by PIDs you recorded
(`Stop-Proc`); never `taskkill /IM`.** Two other lanes (SFX-2, BTN-1) run Godot concurrently; use
`Enter-SuiteMutex` for every launch. Never touch any `sfgd-*` worktree but `sfgd-int`.

# PACKET INT-0 — the wave-1 integration branch (FP-1 + ROUND-1; CARRY-1 when it lands)

Program: `C:/repos/PROGRAM-2026-09-19-SUPERMARKET.md` §3. Orchestrator: FABLE_SUPERMARKET. Role:
programming / integration (Opus). PR: none. Repo `C:/repos/super-find-great-deal`.

## Why this packet exists

The orchestrator's session cannot run `git merge` or `git push` (the auto-mode classifier denies
them), so `main` still sits at the initial commit. Wave-2 lanes need one base that contains BASE-1
+ FP-1 + ROUND-1. You make that base.

## Steps

1. `git fetch origin`. Create worktree `C:/repos/sfgd-int` on a new branch
   `integration/2026-09-19-mvp` from `origin/feat/2026-09-19-round-1` (`2990f69`, which is
   BASE-1 `8c372f2` + ROUND-1).
2. `git merge origin/feat/2026-09-19-fp-1` (`5db3bf5`). Expected overlap (three files, both lanes
   appended to them): `.claude/rules/test-suite.md` (keep both lanes' dated measured sections),
   `scripts/LaunchOptions.cs` (keep both lanes' flags: FP-1's `--fp-look`, `--first-person-cam`;
   ROUND-1's `--round-script`), `tests/Run-AllTests.ps1` (keep both new suites registered; bot
   suites stay in join order, new ones appended at the end). Resolve by keeping both, never by
   dropping one. Read both lanes' handoffs (`docs/agents/handoffs/2026-09-19-FP-1.md`,
   `2026-09-19-ROUND-1.md`) so you know what each added.
3. Build; `dotnet test tests/unit/SailNet.Tests.csproj` — raw count (expect ≥ 1296 + FP-1's 2).
4. Run standalone with `-SkipBuild`, via the suite mutex, one at a time:
   `Run-FirstPersonTest.ps1` (needs a desktop session), `Run-RoundLoopSmoke.ps1`,
   `Run-CarryNetTest.ps1`, `Run-MultiplayerTest.ps1 -BotCount 2`. Quote the raw lines. Do NOT run
   the full marathon (that is INT-1's).
5. **Cross-lane check** the merge cannot see: ROUND-1's teleports move a first-person player. Launch
   server + two `--first-person-cam` bots with `--round-script`, and confirm from the log that a
   teleported peer's camera follows (no stale view, no NaN, `RoomTeleport` epoch bump applied on
   the owner) — ROUND-1's smoke used third-person bots. If the FP camera does not follow a
   teleport, fix it on this branch in its own commit and say so.
6. Also merge `main`'s single initial commit if it is not already an ancestor (it is, via BASE-1;
   verify with `git merge-base --is-ancestor`).
7. Delete `.github/workflows/build.yml` in its own commit ("CI off — Talon, 2026-09-19: never
   passed, only emails; disabled on GitHub, removed here so no branch push ever triggers it").
8. Push `integration/2026-09-19-mvp`. Write `docs/agents/handoffs/2026-09-19-INT-0.md`: merge
   SHA, what conflicted and how it was kept, raw counts, the cross-lane result.

Commit by path; never `git add -A`; attribution lines. One Godot at a time via the mutex; CARRY-1
is still running in `C:/repos/sfgd-carry1` — never touch it.

## Do not

- Do not merge CARRY-1 (it has not landed). The orchestrator re-dispatches a follow-up when it does.
- Do not touch `main`.

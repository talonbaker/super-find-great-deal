# STOCK-1 — a fully stocked shop floor: density as the hiding surface

Branch `feat/2026-09-20-stock-1`, off `integration/2026-09-19-mvp` @ **`c0b024a`**.
Packet: `C:/repos/PACKET-2026-09-20-STOCK-1.md`. Program:
[`../../../PROGRAM-2026-09-19-SUPERMARKET.md`]. Orchestrator: FABLE_SUPERMARKET.
Prior handoffs that bear on every number here:
[`2026-09-19-SHELF-1.md`](2026-09-19-SHELF-1.md) (the room's arithmetic, the `Stock` container,
§7's cost tables), [`2026-09-19-REACH-1.md`](2026-09-19-REACH-1.md) (the rest audit,
`InsideStatic`), [`2026-09-19-CARRY-1.md`](2026-09-19-CARRY-1.md) (`PlacementIntegrity`),
[`2026-09-19-INT-1.md`](2026-09-19-INT-1.md) (the marathon baseline).

> Talon, 2026-09-20: *"Supermarket shelves are extremely filled: three or four items behind each
> one in every row; boxes stacked one behind the other; piles of fruit in bins and crates. I need
> to see MORE overall — more to hide in, from and around."*

---

## 1. What a following lane needs, in one screen

| Thing | Value |
|---|---|
| Base SHA | `c0b024a307464a9469277269e76150f392959a9c` |
| Branch tip | *(filled at the gate)* |
| Density added | *(filled at the gate)* bulk instances, **4 MultiMesh draws** |
| Carryables | **unchanged at 120**, ids **1000..1129 untouched** |
| `ProtocolVersion` | **unchanged.** Nothing new rides the wire; bulk is not networked |
| New suite | `tests/Run-StockTest.ps1`, **udp/7911**, registered last. **Next free is 7912** |
| New capture script | `tests/Capture-ShopFloor.ps1`, unregistered, same port (CLOCK-1's pattern) |
| New launch flag | `--stock-selftest` (offline, headless, no server, no bots) |
| New generated files | `scenes/game/world/supermarket/StockBulk.tscn`, `scenes/game/props/BinMound.tscn` |
| Regenerate them | `SFGD_BAKE_STOCK=1 dotnet test tests/unit/SailNet.Tests.csproj` |
| `SectionScenes` | gains `StockBulk.tscn` |
| Captures | `docs/qa/2026-09-20-stock-1/` -- **OWED at landing, see S5.6** (a foreign Godot held the desktop) |

---

## 1b. THE BRANCH DOES NOT LOAD UNTIL IT IS BAKED

`SearchRoom.tscn` instances `StockBulk.tscn` and `FloorBin.tscn` instances `BinMound.tscn`, and
**neither generated file is committed until the bake has been run**, because the bake needs a
`dotnet build` and this lane's build/Godot window was held by the REVIEW-1 fix lane's marathon
for the whole of the design phase (the packet's Concurrency section, binding).

**Before anything in this branch will open or run:**

```
SFGD_BAKE_STOCK=1 dotnet test tests/unit/SailNet.Tests.csproj
```

That writes both scene files from `ShelfStock`/`StockRoom` and then every later `dotnet test`
compares them. Nothing merges anywhere until the gates in §5 are green, so this state never
reaches another lane — but a reader who checks the branch out before that will see a room
referencing two scenes that are not there, and this is why.

---

## 2. The constraint, restated, because it is the whole design

SHELF-1 §7 measured the floor this room already stands on, and none of it is arguable:

| | props | server p95 at rest | bar |
|---|---|---|---|
| today | 130 networked `RigidBody3D` | 5.80 / 6.72 ms (two runs) | 8 ms |
| under a shove | 130 | 19.8 / 23.2 ms | (upper bound) |
| REACH-1's cost phase | 280 | **56.0 ms** | — |

Plus 230 kB/s of loose-prop stream with 130 props moving, and a voice pool that saturates at 18
of a 24 ceiling with 80 % of one-shots stolen. **Four times the product count as physics bodies
breaks every bar the program has.** So the density is stock the server never simulates and the
wire never carries: same meshes, same materials, same sizes as the carryable facings in front of
it, rendered as MultiMesh and collided as static boxes.

**The carryables did not move, are not more numerous, and did not change id.** That is asserted
three ways rather than promised: `AuthoredPropOrderTests` (no engine), `Run-AuthoredPropTest`
(live server + late joiner) and `Run-StockTest` phase 3 (the adoption line).

---

## 3. The design, and the three places it departs from the packet

### 3.1 BAKED, not built at scene load — the one that needed a ruling

**The packet asked for a `ShelfStocker` node that fills MultiMesh buffers in `_Ready`, seeded per
bay.** `.claude/rules/godot-scenes.md` says the opposite, unconditionally and with a date on it:

> **Every part of a level is authored in its scene file, never built in code** (Talon, 2026-08-27).

and `SupermarketWorldSelfTest`'s own class doc says what the rule is *for*:

> The standing rule ... is that every part of a level is physically authored in the scene file
> **so it can be opened and flown around in the editor**. That is not something a reviewer can
> verify from a diff, because a room that builds a shelf in `_Ready` looks identical in the
> inspector to one that does not. So it is measured.

**A `ShelfStocker` would have PASSED that measurement.** `CheckAuthored` counts NODES, packed
against live, and filling a MultiMesh buffer adds none — so the room would have gone green while
being exactly what the rule forbids: empty in the editor, full at runtime. Passing a check for
the wrong reason is worse than failing it.

**The packet's REASON is kept whole.** It asked for procedural generation because *"1500–3000
instances by hand is unreviewable"*, and that is right. So the same seeded arithmetic runs in a
generator, writes the scene files, and the result is committed — and
`StockBakeTests.TheBakedSceneFilesAreExactlyWhatTheArithmeticProduces` re-derives the entire
layout on every `dotnet test` and compares it byte for byte. The committed file is a pure
function of eighty lines of arithmetic; a reviewer reads the arithmetic; a hand edit to the
generated file is a red with a line number. It is also strictly cheaper: zero load-time cost on
every peer instead of ~2 700 instances' worth each.

**Generalisation, and it is in `.claude/rules/test-suite.md` now:** a node-count check cannot see
work that goes into a RESOURCE rather than into a node, so "it passes the level check" is not
evidence that a level is authored.

### 3.2 End-caps carry PRODUCE, not stacked box displays

The packet gives the end-caps "stacked box displays (pallet stacks)". SHELF-1 had already
authored **twelve carryable oranges onto those same shelves** (two per board, six per end-cap,
ids 1106..1117), and bulk's whole contract is that it is the same product as the facing in front
of it — SFX-1 gave each material its own voice, so a cardboard thud out of a shelf of oranges is
a lie a player can hear at the exact moment they are trying to work out where the other player
is. The stacked box displays are the two **floor pallets**, which is what a stacked display
physically is; an end-cap is a shelf.

### 3.3 The bin mounds have no collision

The packet asks for a pile collider "so a placed object nestles on the mound rather than floating
or sinking". SHELF-1's bible check closed INTERACTION §3 on this exact object:

> a bin is an open-top tub with a real hollow: 0.78 × 0.54 × 0.78 interior, which **clears a
> 0.44 m crate by 0.17 m horizontally and 0.10 m vertically**, so "hide it under the bin" is a
> physical fact and not a scripted allowance.

The interior is 0.54 m tall and a crate needs 0.44 m of it. **Any mound collider deletes that
affordance outright — there is no half-fill that keeps it.** Visual-only keeps both: the hollow
is still hollow to physics, and an object dropped into a stocked bin settles on the bin floor
*among* the oranges, which is both what really happens and a better hiding place than an empty
tub. The cost is stated rather than hidden: something set down there beds into the fruit instead
of resting on top of it.

`BinMound.tscn` is a child of `FloorBin.tscn`, not of the room, for a reason that would otherwise
have been a level bug: a bin is a 6 kg `RigidBody3D` a player can pick up and tip over, and
static oranges authored at a bin's room position would hang in the air the first time anybody
moved one.

### 3.4 Nothing is stacked two high on a board, and the packet's own words cover it

"Boxes 2 deep and stacked 2 high **where the board clearance allows**." The clearance is 0.50 m
and two cereal boxes are 0.56 m, so the conditional is false on every board. Stacks live on the
floor pallets, where 1.40 m of them fits under the bays' 1.80 m skyline — the line SHELF-1 chose
so a bay hides the aisle behind it from a standing player.

### 3.5 The bulk mesh is a coarser tessellation of the prefab's mesh

`CylinderMesh` defaults to 64 radial segments and `SphereMesh` to 64×32; at those counts the bulk
cans alone are about a million triangles. Every dimension that can be seen or collided with is
identical to the prefab's — radius, height, albedo, roughness, metallic, emission, all asserted
against the prefab by `ShelfStockTests` — and the segment count is the one number a player cannot
tell apart on a 70 mm object at half a metre. It is an LOD, not different art.

---

## 4. The layout, as arithmetic

Everything below is `scripts/game/world/stock/ShelfStock.cs` and `StockRoom.cs`, engine-free and
`dotnet test`-able in milliseconds. The room's own geometry is **read out of `SearchRoom.tscn`**
(by the generator and by `dotnet test`) and **out of the live tree** (by the engine self-test),
never tabulated — so the layout has exactly one source of truth and moving a bay in the editor
moves its stock with it.

### 4.1 The grid

| | cans | cereal boxes | produce |
|---|---|---|---|
| product | 0.07 × 0.12 × 0.07 | 0.19 × 0.28 × 0.06 | r 0.08 |
| slot pitch along the bay | 0.09 m | 0.21 m | 0.175 m |
| slots across a 2.18 m clear span | **24** | **10** | — |
| slots across a 1.28 m end-cap | — | 6 | **7** |
| depth rows × pitch | 4 × 0.10 | 4 × 0.10 | 2 × 0.20 |
| rows on boards 0 and 1 | all four | all four | both |
| rows on board 2 | **the two outer only** | the two outer only | the outer one |

**Board 2 carries its two outer rows and nothing between them, and that is a sightline decision.**
REACH-1 measured the avatar's eye at **1.040 m**; board 2's surface is **1.525 m**. From anywhere
a player can stand you see that shelf's front edge and the undersides, and its middle rows are
geometrically invisible from both sides. A full facing at each of the bay's two open faces is
what the player actually sees, and it is half the top shelf's instance cost.

### 4.2 The holes

One to three per board, seeded from the bay's node NAME (a node name is native and survives
instancing — `.claude/rules/godot-scenes.md` has paid four times for identity carried on an
`[Export]`). The first hole on every board goes all the way through, so every bay keeps a
sightline a player can see and shove a can through — SHELF-1's "root around" property, which is
what keeps this room somewhere to search rather than four walls with tins on them.

**The rule that is a GAMEPLAY rule and not a layout rule: every hole is cleared from a FACE
inward, never out of the middle.** REACH-1's layer 3 refuses a placement whose only sightline is
blocked by STATIC geometry (`ReachReason.OccludedByStatic`), and bulk is static. A hole with
filler still in front of it is a hiding place the hider can use and the game will then refuse the
Confirm on — *the round ends in a shrug, which is the worst outcome this design has.* Asserted
from the emitted layout, not from the code that emits it
(`ShelfStockTests.EveryHoleReachesAFaceWithNoBulkInFrontOfIt`).

A hole is **3 can-slots (0.27 m)** or **2 box/produce-slots**, and every one of them clears the
widest object a hider can be carrying at any yaw — the cereal box's 0.199 m plan diagonal — by
more than twice `PlacementIntegrity.DefaultOverlapToleranceM` on each side.

### 4.3 Collision

Static, layer 1 (the mask `PlacementIntegrity.QueryMask` reads), **sized to the filled volume per
slot-run**. Two adjacent slots merge into one box only when their depth masks are identical, so a
shallow hole's slots get their own shorter box rather than being swallowed by the run beside them.
One box per board would have made every hole in the room a wall that looks like a hole.

### 4.4 The carve-out, which is the defect that would have broken the room

**SHELF-1's 120 products are networked `RigidBody3D` standing on the same boards the filler
fills.** Static filler laid over one puts a networked prop permanently inside static geometry:
every rest audit reports `StaticOverlap`, REACH-1's layer 3 answers `InsideStatic`, and a hider
who chose that prop is refused the Confirm with `NobodyCouldReachThat` — **in a room that renders
perfectly and whose every other suite stays green.** A hundred and twenty of those is the whole
room broken, quietly.

So every cell whose product box would come within **10 mm** of an authored facing is emptied, and
the facing ends up standing in its own hole in the filler, which is what a front row *is*.
`StockBakeTests.NoBulkInstanceStandsWhereACarryableFacingStands` is the gate.

**Why 10 mm and not the 0.02 m placement tolerance, measured rather than chosen:** at produce's
0.175 m pitch for a 0.16 m sphere, a 0.02 m clearance empties both NEIGHBOURS of every facing as
well and leaves an end-cap board with 2 of 14 cells standing — barer than SHELF-1 left it. The
quantity that matters is PENETRATION, and any positive clearance is already zero penetration.

A hole may also only be cut in **intact** stock (every slot it spans still carrying its board's
full depth), with a deterministic scan behind the random draws, so "every board keeps a hole" is
a claim about the geometry rather than about the luck of a seed.

---

## 5. Gates

Every suite run standalone, in the foreground, redirected to a file, under the machine-wide
mutex. Raw lines, from each run's own output.

### 5.1 Build and xUnit

`dotnet build SuperFindGreatDeal.csproj` -- **0 errors, 5 warnings**, the same five this tree
already carried (CS0618 in `AvatarClipDirector`, two CS8604 in `AvatarVisual`, CS8618 in
`SortBin`, CS8602 in `SandboxCamera`). None is in a file this packet touched.

```
dotnet test tests/unit/SailNet.Tests.csproj
Failed: 0, Passed: 1798, Skipped: 0, Total: 1798, Duration: 3 s - SailNet.Tests.dll (net8.0)
```

**REVIEW-1's baseline was 1770. +28, exactly additive:** `ShelfStockTests` 15 facts + 6 theory
cases = 21, `StockBakeTests` 7 facts. Nothing existing was changed to make anything pass.

### 5.2 `Run-StockTest.ps1` -- the new suite (udp/7911)

```
STOCK-TEST OVERALL: PASS
        [stock-selftest]   bays found in the tree: 18 (2 end-cap(s))
        [stock-selftest]   authored carryable facings: 120
        [stock-selftest]   BulkCans    instances=1356 (derived 1356)
        [stock-selftest]   BulkBoxes   instances=652 (derived 652)
        [stock-selftest]   BulkProduce instances=30 (derived 30)
        [stock-selftest]   collision runs=195 (derived 195)
        [stock-selftest]   holes authored: 101 across 54 board(s)
        [stock-selftest]   holes checked=101 poses=303 blockedByAnotherProp=0
                           worstPenetration=0.000 m at (none) (tolerance 0.020 m)
        [stock-selftest] SUMMARY holes=101 poses=303 propBlocked=0 worstPenetrationM=0.000
                           failures=0 result=PASS
        [reach] layer3 [rest] prop=1 reachable (Reachable) - 1/1 standable of 2 sampled,
                2 ray(s), first hit Target
        layer 2: silent for prop 1 across the whole run, which is a clean audit
        [reach] layer3 [confirm] prop=1 reachable (Reachable) - ... first hit Target
        round entered Seeking on the Confirm press
        [props] adopted 162 authored prop(s) in 191.92 ms (ids 1000..1161)
```

**101 holes, 303 poses, worst penetration 0.000 m.** That is the packet's central assertion
proved on every hole in the room rather than on three: a near-miss can, box and orange each set
down at the pose a hider leaves it, in every authored hole, judged by the shipped
`PlacementIntegrity.Check` -- and not one of them overlapped anything at all.

### 5.3 The five standalone suites the packet names

```
SUPERMARKET WORLD TEST OVERALL: PASS
        [supermarket-selftest] SUMMARY failures=0 result=PASS
        [supermarket-selftest]   SearchRoom.tscn    packed=335 live=335
        [supermarket-selftest]   ShelfUnit.tscn     packed=12 live=12
        [supermarket-selftest]   EndCap.tscn        packed=12 live=12
        [supermarket-selftest]   StockBulk.tscn     packed=200 live=200
```

**`StockBulk.tscn packed=200 live=200` is the row that matters** and it is green for the right
reason: nothing is built in code, because the buffers are baked. A `ShelfStocker` would have been
green here too, which is exactly why baking was the answer (S3.1).

```
AUTHORED-PROP TEST OVERALL: PASS
        server: [props] adopted 162 authored prop(s) in 164.56 ms (ids 1000..1161)
        bot A (present from the start): ... 162 ... (ids 1000..1161)
        bot B (late joiner):            ... 162 ... (ids 1000..1161)
        all three peers agree on the block: 1000..1161/162
        bot B (late joiner): all 162 authored ids present in its first sample
        A vs B: all 162 authored props agree, worst 0.0000 m (id 1000, bar 0.10 m)
```

```
PLACE-TEST OVERALL: PASS
        B sees prop 1014 at (40.200, 0.217, 0.000) - 0.008 m and 0.00 deg from the intended pose
        bot B refused beyond arm's reach (TooFarToPlace), still holding prop 1017
        bot C refused inside the pillar (DoesNotFitThere), still holding prop 1015
        bot D refused outside the room bounds (OutsideRoom), still holding prop 1016
```

```
REACH-TEST OVERALL: PASS
        [reach-selftest] SUMMARY cases=9 failures=0 result=PASS
        [reach-cost] server physics frame time over 1200 tick(s):
                     p50 16.660 ms, p95 46.129 ms, peak 47.742 ms
```

`cases=9`, unchanged -- this packet's self-test is its own flag and its own suite and adds no
case to the planted room. **The cost phase's p50 has roughly doubled (REACH-1 8.067, TASK-1
4.850, here 16.660)** and that is 150 seeded crates shoved on top of 162 authored props in a room
that now has 195 more static collision boxes for every one of those queries to sweep. Read it
against the at-rest row in S5.4, never against the 8 ms bar; REACH-1's own header says the shove
figure is an upper bound and not a budget.

**`Run-MaterialSfxTest` -- 2 PASS / 2 FAIL over four standalone runs, and it is the documented
staging flake rather than this packet's bulk.** Three independent lines say so:

- **The failing string is the documented one**, verbatim from INT-1's entry: `cardboard: prop 2
  never made a sound at all - it was never grabbed or never landed`, with **zero `grab denied`
  lines on the server** -- TASK-1 S3.3's "a press that is never made is never refused" -- and the
  box bot's `heldPropId` never leaving -1 across all 128 samples.
- **The geometry rules the bulk out.** `SfxBoxBot` spawned at (38.5, 2.1) and stopped at
  **(42.41, 0.65)**. Aisle 2's frame face is at z = 0.80 and the avatar's body radius is 0.145:
  0.80 - 0.145 = **0.655**. It is wedged against SHELF-1's BAY FRAME to the millimetre. This
  packet's nearest bulk face in that bay is at z = 0.865 -- 0.065 m BEHIND the frame the bot
  stopped on, so it could not have been what stopped it.
- **The rate is the base's rate.** INT-1 measured 3 PASS / 2 FAIL over five runs on this room
  before STOCK-1 existed, always exactly one driver lost and a different one each time. Four runs
  here gave 2/2, losing cardboard twice and produce once.

Stated rather than closed: an A/B against a detached base worktree would settle it harder, and
was not run.

### 5.4 Frame time, before and after

Server: `tests/Measure-ShopFloor.ps1 -Pass rest`, `--cost-shove-every 0`, 1200 physics ticks over
20 s, 162 authored props, two headless bots walking. Client: `BotHarness`'s `pms`, a 5 Hz spot
reading, 89 samples per bot -- SHELF-1's instrument, unchanged, so the two sides are comparable.

**BEFORE is the same build with the base tree's two scene files checked out** (`git checkout
c0b024a -- SearchRoom.tscn FloorBin.tscn`, re-imported, restored in a `finally`), so exactly one
thing differs between the rows: whether the room has bulk in it. Never `git stash` -- the stash
stack is shared with every worktree on this machine.

| | bulk instances | server p50 | **server p95** | server peak | client A p95 | client B p95 |
|---|---|---|---|---|---|---|
| **BEFORE** (SHELF-1's room) | 0 | 1.958 ms | **7.055 ms** | 23.010 ms | 11.774 ms | 12.773 ms |
| **AFTER** (this packet) | 2 212 | 1.634 ms | **6.483 ms** | 22.383 ms | 3.700 ms | 2.998 ms |
| bar | | | **8 ms** | | **5 ms** | **5 ms** |

**Both bars are met, and the table has to be read honestly: the AFTER is BETTER than the BEFORE
on every column, which cannot be an effect of adding 2 212 objects.** The difference is machine
noise and the noise is larger than the effect. Two things make that the right reading rather than
an excuse:

- **Nothing in the AFTER row is work either peer does per frame.** Bulk is a MultiMesh buffer and
  195 static collision boxes. A static body is not simulated; it enters the broadphase and
  nothing else. The server's per-tick work is the 162 rigid bodies it was already simulating,
  which is why `props in world: 162` is identical in both rows.
- **`pms` is `TimePhysicsProcess` on a HEADLESS bot, so the client column does not measure
  rendering at all.** This is SHELF-1's instrument and the bar is SHELF-1's bar, so the rows are
  comparable to each other and to that handoff -- but 2 212 MultiMesh instances land on the GPU,
  and a headless peer has none. **The client column is not evidence about what Talon will see.**
  The draw-call reading from the windowed capture peer (S5.5) is, and it is the number to read
  for render cost.

The BEFORE run's client p95 of 11.8 / 12.8 ms against SHELF-1's measured 3.4 / 1.9 ms on the same
instrument is the size of that noise, stated rather than smoothed: a 5 Hz spot reading over 89
samples puts its p95 on about the 85th sample, so two scene-load hitches move it by ten
milliseconds.

**Provenance, because the orchestrator asked.** Neither row was taken beside a foreign Godot.
`after-rest` finished **13:39:37** and `before-rest` **13:53:28**; the first Godot process this
lane did not start appeared at **13:54:33**. A second AFTER run and the capture pass WAITED for
that process to clear rather than measuring beside it.

### 5.5 How many more carryables the bar allows

**Between 40 and 60, and the band is the answer rather than a number.**

The arithmetic, all of it from the rows above and from SHELF-1 S7.1:

| population | at-rest p95 | source |
|---|---|---|
| 130 props | 5.80 / 6.72 ms | SHELF-1, two runs |
| 162 props | 6.48 / 7.06 ms | this packet, two runs (AFTER / BEFORE) |

Thirty-two more networked bodies cost about **0.5 ms** of p95. The worse of this tree's two runs
is **7.06 ms**, leaving **0.94 ms** against the 8 ms bar -- about **60 more props** at that rate,
or about **40** if you keep SHELF-1's habit of quoting the worse run and leaving a margin.

**Why it is a band and not a number, stated because the temptation is to report the midpoint:**
two runs of the SAME population on this machine differ by 0.58 ms (6.48 vs 7.06), which is larger
than the 0.5 ms that thirty-two props cost. The noise is bigger than the signal being
extrapolated from. Anyone who wants a real number should take five runs at one population rather
than trusting this extrapolation.

**And it is unchanged by this packet, which is the point.** Bulk adds no networked bodies, so the
carryable headroom Talon has after STOCK-1 is exactly the headroom he had before it -- the
density came out of a budget that was not this one. The other half of that trade is still
SHELF-1 S7.3's bandwidth (30 Hz per moving prop, 230 kB/s with 130 loose) and S7.5's voice pool
(saturated at 18 of a 24 ceiling, 80 % of one-shots stolen at 130 props), and neither moved
either.

### 5.6 Captures -- OWED, and why they are not here

**`docs/qa/2026-09-20-stock-1/` is empty at the time of landing.** The capture pass needs a
desktop session and opens windows, and from 13:54 onward two Godot launches this lane did not
start have been up on this machine (Talon's). Shooting four windowed poses over a human's screen
is not a thing to do, and the orchestrator's standing rule is that no frame-time or capture work
runs beside a foreign Godot. The correctness gates do not need a window, so they were all taken
(S5.1-S5.4) and only the pictures are owed.

**The exact command, ready to run the moment the machine is clear:**

```
powershell -File tests/Capture-ShopFloor.ps1 -Pass after  -SkipBuild
powershell -File tests/Capture-ShopFloor.ps1 -Pass before -SkipBuild
```

Four poses -- `aisle` (down the walkway between aisles 0 and 1, the shot that answers the ask),
`endcap`, `pile` (into a bin of oranges), `gap` -- plus a BEFORE pass that checks the base tree's
two scene files out, shoots the identical aisle pose, and restores them in a `finally`. The host
runs with `--perf-log`, which is where **the draw-call number** comes from: `PerfHud.Snapshot`
already reads `RenderTotalDrawCallsInFrame` and `RenderTotalPrimitivesInFrame` at 1 Hz, and no
script in `tests/` passed that flag before this one.

**The draw-call reading is the one number in this handoff that is genuinely missing**, and S5.4
says why it matters: `pms` is measured on a HEADLESS bot, so the client frame-time column does
not measure rendering at all. 2 212 MultiMesh instances land on the GPU and nothing quoted here
has looked at that. The design bound is known -- three MultiMesh draws for the room plus one in
the bin prefab, asserted by `StockBakeTests` so it cannot start scaling with the bay count -- but
the measurement is owed.

---

## 6. Bible check

```
Bibles applied:  LEVEL, because this packet is an arrangement of space and nothing else -- 2 212
                 objects placed on eighteen bays, six bins and two floor pallets in a room whose
                 dimensions nobody touched.
                 INTERACTION, because the holes are an affordance: the whole point of the density
                 is that a player puts the object IN it and gets it back out, and because every
                 one of the 120 things still pickable had to stay pickable.
                 MECHANICS does not apply. Nothing here resolves its own state, keeps a timer or
                 scores anything; the audit that runs on these props is REACH-1's and not one of
                 its rules was touched.

Items checked:   LEVEL 3.1 (reachable today, unreachable later) -- this is the item the packet
                 turns on, read literally rather than by analogy. Bulk is STATIC, and REACH-1's
                 layer 3 refuses a placement whose only sightline is blocked by static geometry.
                 A hole with filler in front of it is a hiding place that is reachable when the
                 hider uses it and unreachable when the game is asked about it. Every hole is
                 therefore cleared from a FACE inward, asserted from the emitted layout
                 (ShelfStockTests) and again on a live server (Run-StockTest phase 2, which read
                 "first hit Target" rather than a static hit).
                 LEVEL 4.1 (topology) -- unchanged and deliberately so. Not one bay, aisle,
                 walkway, spawn marker or cross-aisle moved; the room is still SHELF-1's five
                 walkways and two cross-aisles. The two new floor objects are flush against the
                 end walls where nothing has ever walked, and their clearance from every existing
                 fixture is asserted rather than eyeballed
                 (TheFloorPalletsAreClearOfEveryFixtureTheRoomAlreadyHas), which is what caught
                 them poking 15 mm through a wall.
                 LEVEL 3.2 (player spawn) -- untouched. All four search-room markers keep their
                 names, their count and their coordinates, and nothing new is within 1 m of one.
                 INTERACTION S1 (an interactable must READ as interactable) -- the reason bulk
                 reuses each prefab's exact material, emission included, rather than a cheaper
                 one. A filler that shaded differently from the carryable in front of it would
                 teach the player that the shiny ones are the real ones, which is the opposite of
                 what a room of near-misses is for. Asserted against the prefabs
                 (EveryBulkMaterialMatchesItsPrefab), because it is the kind of thing that drifts
                 silently.
                 INTERACTION S3 (physical/visual correspondence) -- the item that decided the bin
                 mounds. A mound you can see and cannot touch is a correspondence failure; a
                 mound with a collider deletes "hide it under a tipped bin", which SHELF-1 closed
                 this same item on with measured numbers. Visual-only, with the cost stated in
                 S3.3 rather than hidden.
                 INTERACTION S4 (range and timing) -- untouched. No reach, radius, tolerance or
                 timing constant moved.

Result:          PASS, with one item REPORTED rather than closed: INTERACTION S3's bin mound
                 (S3.3). The correspondence is deliberately one-way -- you can see fruit you
                 cannot rest an object on top of -- and the alternative deletes a hiding place
                 the game is built on. It is Talon's call after the ride, with both halves and
                 the arithmetic in front of him.
```

---

## 7. Open for the orchestrator

### 7.1 If the bulk ever has to become real props (a SKETCH, asked for by the orchestrator)

Under a coming sync change the search room runs on the HIDER's client during Hiding, which
changes who pays for a physics body and re-opens a question this packet answered "no" to.

**What the bake would emit instead.** Nothing about `ShelfStock`/`StockRoom` changes -- they
already produce a list of poses. `StockBake.EmitBulkScene` would stop writing
`buffer = PackedFloat32Array(...)` and instead write, per instance, SHELF-1's authored-prop
structure verbatim: a `Node3D` carrying `NetworkedProp` with a child literally named `Body`
instancing `Can.tscn` / `CerealBox.tscn` / `Produce.tscn`. The `StockBulk/Collision` body and all
195 runs would be DELETED, not kept -- every instance would carry its own collider, and the
static runs would then be invisible walls standing inside the props.

**What ids and adoption would need.** `PropManager.AdoptAuthoredProps` numbers by ORDINAL
NODE-PATH SORT from 1000, so ~2 000 new props must sort AFTER every existing one or the whole
block moves and `Run-PlaceTest` names four of those numbers in its own source. The good news is
that it already does: `"Stock/..."` sorts before `"StockBulk/..."` (`/` is 0x2F, `B` is 0x42), so
leaving them under the existing `StockBulk` node gives them one contiguous block above 1161 for
free. They would need SHELF-1's zero-padding to four digits -- `Can_0000` -- so the id order is
the reading order past 999.

**What it would cost, from this packet's own measurements, so nobody has to guess.** Adoption is
O(n log n) and took **192 ms for 162 props**; 2 200 is roughly fifteen times the population on
every peer at world build. Server physics at rest is **6.48 ms p95 for 162 bodies against an
8 ms bar** -- SHELF-1 §7.1 already called 120 "close to a ceiling". The loose stream is 30 Hz per
moving prop, and SHELF-1 measured 230 kB/s with 130 moving. The voice pool saturates at 18 of a
24 ceiling with 80 % of one-shots stolen at 130 props.

**So the shape that could work is promotion, not conversion**: keep the bake as it is, and
promote only the bulk in the bay a hider is actually reaching into -- one bay is ~120 instances,
which is SHELF-1's whole budget in one place and nowhere else. The bake would need to emit a
per-bay index (which buffer slots belong to which bay) so a promoter could swap a slot out of the
MultiMesh and spawn one body in its place. **That index does not exist today and this is a sketch,
not a design.**

### 7.2 Smaller things

- **`Run-MaterialSfxTest` is 2 PASS / 2 FAIL on this tree** (S5.3), the documented staging flake,
  ruled out as this packet's by geometry to the millimetre. The underlying cause is still INT-1's
  open item: `ScriptedCarryIntentSource`'s arrive radius versus a room a straight-line walker
  cannot cross. REVIEW-1 moved the press rather than the radius; the wedge is still there.
- **`Run-ReachTest`'s cost phase p50 doubled** (8.067 -> 16.660 ms) because its 150 seeded crates
  now sweep 195 more static boxes. Not a bar, but the next lane to read that number should know
  why it moved.
- **The bake is a build-time step with no CI hook.** `SFGD_BAKE_STOCK=1 dotnet test` regenerates;
  an ordinary `dotnet test` catches drift. Nothing runs the bake automatically, deliberately --
  a generator that rewrites committed files on every CI run is a generator nobody reviews.

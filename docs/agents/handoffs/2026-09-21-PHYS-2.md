# PHYS-2 — the shove is a number, and the three reds have owners

Branch `feat/2026-09-20-phys-1` (continued, not forked), tip **`d8f024b`**, pushed. Base for every
A/B in this document: a detached worktree `C:/repos/sfgd-phys2-base`, moved between
**`f086ed4`** (FEEL-1's tip), **`e75e04d`** (`origin/integration/2026-09-19-mvp`, still the
integration tip at the time of writing — INT-2 part A has not landed there) and **`1511ea8`**, a
synthetic, unreferenced merge of those two (FEEL-1 + STOCK-1 with no PROBE-1/PHYS-1/PHYS-2 in it —
`git merge-tree` reported it clean). PHYS-1's handoff is `2026-09-20-PHYS-1.md`; its §10–§11 owed
list is this packet's specification, and Talon's sentence at the top of that file still is.

---

## What a following lane needs, in one screen

| Thing | Value |
|---|---|
| The shove is the suite's | `--phys-shove "propId,vx,vy,vz,atSec[,wx,wy,wz][;...]"` → `PropManager.ServerNudgeLoose(id, v, angular)`. Server clock, same one `--seed-props-drop` uses |
| The spin is named, never drawn | `ServerNudgeLoose(id, v, null)` is REACH-1's random ±2 rad/s tumble, byte for byte, for its two old callers; `--phys-shove` passes `Vector3.Zero` or the triple you typed |
| An upright can skids | μ 0.25 < r/h 0.58, so a 1 m/s shove on its end travels **0.20 m**. The suite lays the can down (+X, 6 rad/s) and rolls it three seconds later at 1 m/s with ω_z = −v/r |
| `Run-PhysicsFeelTest` staging | three `[phys] shove` lines or the run is STAGING, not a verdict. Bar (1)'s window ends at the shoved box's first movement, read off the props |
| The three PHYS-1 reds | `Carry: hold` — FEEL-1's · `Sfx: material voices` — spawn-pin collision, `Gameplay.ApplySpawnPins` · `Stock: authored holes` — **PHYS-1's**, `assets/physics/tin.tres` (§3) |
| Why the row did not fall, and what made it | §1b: a frozen neighbour eats the first touch. **`PropManager.ServerWakeIsland`** wakes every Resting prop within `WakeIslandGapM` (0.15 m, world-axis boxes, cap `WakeIslandMax` 16) of any prop that wakes, with no impulse — logged `[phys] wake-island`, not counted as a P1 wake |
| A domino is a geometry | a cereal box tips at 24° across its width and 8° across its depth. The suite's row stands EDGE-ON (`--seed-test-props` sixth field, yaw 90) at 0.10 m pitch; the fifth field (roll about X) seeds the can lying down |
| Bounds are exact per shape now | `PlacementIntegrity.ExtentsInside` via `PropPhysics.RoundExtents` / `BoxExtents` — a rolling can or orange is no longer restored home for rotating |
| xUnit | **Failed: 0, Passed: 1944, Skipped: 0, Total: 1944** |
| Captures | `docs/qa/2026-09-21-phys-2/` (§6) |
| Ports | 7916 (suite + capture harness, as assigned). 7902 and 7913 were used in this session for CarryHold/MaterialSfx re-runs on the base — those suites' own ports |

---

## 1. What changed on the tree (three commits)

**`16608ce` — the shove is a number the suite chooses.** PHYS-1 §11.3 measured the walking bot
delivering 0.66–3.30 m/s into the row across six runs of one build; the chain came out 2/5–5/5 with
every other bar stable to two decimals. `--phys-shove` hands REACH-1's `ServerNudgeLoose` a velocity
the suite chooses; it wakes nothing by contact and computes no impulse, so the `[phys] wake` count
stays the evidence that P1 ran — every box after the first is woken by the shipped contact path.
TailBot and PhysBot's walk are gone; PhysBot holds a crate six metres west for bars (1) and (6).

**`152a44b` — a shove that draws its own spin is still a coin flip.** Found by reading the funnel,
not by six more runs: `Carryable.Release(Vector3)` draws ±2 rad/s per axis, a box goes over at
~4 rad/s, so the draw alone decided whether a shoved box toppled, slid or fell backwards. The
overload names the spin; the two existing callers get `null` and the old behaviour.

**`d8f024b` — a can standing on its end does not roll, it skids.** Arithmetic before any run:
tipping a can needs μ > r/h = 0.58 and tin's is 0.25, so PHYS-1's 3.32 m "roll" was a can the bot
had knocked over first. `--phys-shove` grew an optional angular triple — all three or none, because a
partial triple that quietly became `(wx, 0, 0)` would stage a different event under the same command
line — and the suite spends it twice on one prop.

### 1b. Then the suite was run on the tip, three times, and the row did not fall (2026-09-21, later)

`Run-PhysicsFeelTest` on `d8f024b`: **0/3**, and not for a staging reason — both shoves fired,
`wakes=1..5`, and the readings were `not one of the five boxes tilted past 60 deg` (both views),
`the can travelled 0.25–0.70 m (bar 1.0)`, plus one run with `restores=1`. PHYS-1's 5/5 (§9cf7ca5)
was two BOTS walking through the row, and its own commit says so ("one mover could not do it");
box-on-box had never propagated. Four defects, each measured, each with its own fix on this branch:

| what the log said | what it was | the fix |
|---|---|---|
| `wake prop=2 at=2.77 -> 1.66`, `prop=3 1.65 -> 0.99`, `prop=4 0.57 -> 0.34`, no tilt | the approach speed was projected from `LinearVelocity` inside `body_entered`, which for a head-on hit on a frozen prop is the STOPPED body — SFX-1's own one-sided failure, one signal over | `Carryable.ApproachVelocity` (the vector beside SFX-1's `ApproachSpeedMps`), projected in `NetworkedProp.ReportBump` |
| same wakes, same speeds, still no tilt | the impulse landed at the struck prop's CENTRE — pure translation; a domino is knocked over by its neighbour's top edge | `PropPhysics.StrikePoint`: the mover's support point along the line of centres (top-front edge when toppling, face centre when sliding) |
| every box woken, every box stopped 2 cm later | a Resting prop is frozen KINEMATIC — infinite mass — so the mover's momentum is eaten in the step that reports the contact, and the wake hands the neighbour only `ContactImpulse`'s share | two layers. (a) When the neighbour woke, the mover is given back `ApproachVelocity − spent/m` and its spin (`ReportBump`; `ServerBumpProp` returns the impulse it spent) — measured alone, still no tilt, because the neighbour is frozen for the whole first step and there is no second. (b) **`ServerWakeIsland`**: the moment any prop wakes (shove, contact, crate, avatar) every Resting prop within 0.15 m of its world-axis box wakes too, no impulse, wave through their neighbours, cap 16 — so what the mover hits is a body. Measured: the row went dynamic end to end (`wake-island prop=2..5`), and the engine did the rest |
| island in, boxes at 2 cm: the row slid 8/6/4/2 cm as a block, no tilt | correct physics for TOUCHING boxes: a box can lean 4° before it meets the next, so five 0.19 m-wide boxes at 2 cm are a stack of books | — (a fact about the fixture, recorded in the suite's header) |
| island in, boxes at 11 cm across their width: 52° / 23° / 7°, then frozen leaning at 33 / 20 / 4 | a 0.19 × 0.28 box is committed at 24° and hands its neighbour a lean, not a fall; the settle latch then froze the lean | the row stands **edge-on** — 0.06 m deep along the row, committed at 8° — at 0.10 m pitch. `--seed-test-props` grew a sixth field (yaw about Y) to say so. **5/5 on both views, `all within 0.00s`** (run 6) |
| `layer2 prop=6 OutOfBounds -> RestoredLastGood` on a can rolling on the floor, and `prop=1135` (an authored orange) restored 0.4 m in the same run | REACH-1's bounds test swept the eight corners of the shape's LOCAL box: a cylinder rolled 45° reads √2 too tall, a sphere √3. A can at its measured floor rest (1.2 cm in) was 2.6 cm "outside" the room | `PropPhysics.RoundExtents` / `BoxExtents`; `PlacementIntegrity.ExtentsInside` uses the true extent per shape |

The island wake is the one change here that is design rather than repair, and it is stated as
such in §8: P1 said "a resting prop stops being an immovable wall", PHYS-1 built the wake, and
a frozen kinematic body is still a wall for the one step that matters. Waking the neighbourhood
before it is struck is the smallest thing that makes a frozen Resting prop behave like a sleeping
dynamic one without changing what Resting is on the wire. Cost: one settle audit per island wake,
0.3 s later; a room nobody touches sees none (the at-rest table in §5 has `wakes=0`, and an island
cannot start without a wake).

And one staging fact: **an upright can cannot be laid down by a spin.** Friction 0.25 < r/h 0.58,
so the 6 rad/s lay-down skidded the base out and the can wobbled and stood (tilt peaked at 1°,
three runs). `--seed-test-props` grew an optional fifth field — degrees of roll about X — and
both the suite and the capture harness seed the can already on its side. The suite's shove count
is two now, not three. The domino row's `wakes=` reads as the CONTACT wakes only — with the
island in, the row's boxes are woken before they are struck, so a green run shows `wakes=1..3`
(CARRY-1's crate the bot leans on, and whatever the rolled can meets) and `wake-island` lines for
the row; the `not one [phys] wake` staging bar still fires on a tree where P1 is gone, because the
island itself is only ever started by a wake. `PhysStrikeExtentsSeedTests` pins the three pieces of arithmetic and the
fifth field's drop-on-malformed rule; xUnit **Failed: 0, Passed: 1957, Skipped: 0, Total: 1957**.

{FEEL_FIX_RUNS}

---

## 2. The three reds, discriminated

Each re-run **at least three times a side, interleaved**, on the base worktree; verdicts are the
suites' own `OVERALL` lines and the quantity is quoted. The full table, with coordinates, is in
`.claude/rules/test-suite.md` (PHYS-2 section). Summary:

| suite | PHYS-2 `d8f024b` | base | owner |
|---|---|---|---|
| `Run-CarryHoldTest` | 4/6 PASS | `f086ed4`: 5/6 PASS | **FEEL-1.** All three reds are `-0.0435 / -0.0435 / -0.0441 m`, one tick of 77, `t≈12.0 s`, prop at `(35.50, 0.67, -2.40)` — with and without PHYS. The witness's clearance is positive in all twelve runs, so it is the holder's own capsule projection at the twelfth second of `HoldStressIntentSource` |
| `Run-MaterialSfxTest` | 2/3 PASS | `e75e04d`: 3/3 PASS | **Whoever owns spawn pins** (`Gameplay.cs` has no diff on this branch). §2.1 |
| `Run-StockTest` | 0/3 PASS | `e75e04d`: 3/3 · `1511ea8` (FEEL-1+STOCK-1, no PHYS): 3/3 | **PHYS-1.** §3 |

PHYS-1's `crossed wires: CardThud from Bin_4` shape did not recur in any of the six MaterialSfx runs.

### 2.1 `Sfx: material voices` — a third way to lose one driver, and it is a spawn-PIN collision

INT-1's rules entry ruled out SHELF-1's join-order theory and measured the 1.49 m wedge; REVIEW-1
measured the client's 1.5 m bar. This is neither. From the failing run's own traces:

```
17:07:33.908  peer joined  1099405582 (SfxProduceBot)  players=2   <- 2nd joiner: join-order marker 1 (44.5, 0)
17:07:34.165  peer joined  1954162340 (SfxWitness)     players=4   <- 4th joiner: join-order marker 3 (41.5, -2.1)
17:07:34.415  spawn pinned SfxProduceBot marker=3 at=(41.50,1.10,-2.10) moved=True
17:07:34.669  spawn pinned SfxPlaceBot   marker=1 ...
17:07:35.169  spawn pinned SfxWitness    marker=1 at=(44.50,1.10,0.00)  moved=True   <- 750 ms after the produce bot
```

`ApplySpawnPins` pins a name the frame it replicates. The produce bot's name arrived first, so it
was moved onto marker 3 **while the still-unnamed witness was standing on it by join order.** Both
peers' logs agree on what followed: the produce bot at `(41.5, 2.30, -2.1)` — on the witness's
head — for 0.6 s, the witness at `(41.46, 0.82, -2.03)` under it; when the witness was pinned away
the produce bot came off into the +Z edge walkway at `(39.97, 0, 4.8)`, walked its straight line
toward `(38, -2.1)` and stopped against the aisle face at `(38.09, 0, 3.55)`, **5.7 m from its
prop**, `heldPropId = -1` all run, no `[phys] wake prop=3`, no `ProducePick`. In the passing runs
the produce bot joined 4th, so the pin moved it onto the marker it was already on.

**Discriminator for the next reader — the lost driver's FIRST sample:** `y ≈ 2.3` at a marker's x/z
is this (it is standing on somebody); a resting x/z 1.49 m from the prop is INT-1's wedge; y = 0 in
another walkway is a bot displaced before it walked. The fix is one of two shapes in
`Gameplay.ApplySpawnPins` — pin in one pass once every expected name is known, or pin the displaced
peer first — and neither is a physics change, so neither is done here.

---

## 3. `Run-StockTest` is PHYS-1's, and the file is `assets/physics/tin.tres`

Deterministic to the millimetre on this tree, three of three:

```
[reach] layer2 prop=1 StaticOverlap -> Depenetrated from=(35.87, 0.48, -3.15) to=(35.88, 0.51, -3.15)
        depth=0.032m pushed 0.037 m; penetrates /root/Gameplay/World/SearchRoom/Aisle0_Bay0 by 0.032 m (tolerance 0.020 m)
```

What is the same on every tree: the seeded pose (`35.875,0.485,-3.150,can`, printed by phase 1 of
every run), the audit (`ServerAuditRest`, `RestAudit`, `PlacementIntegrity` — no diff), the drop
(`StepSeededDrop` — no diff), and the pose the can settles to before the audit fires (`y = 0.478`,
sunk 7 mm from the seed, on every tree). What differs is the number `CollideShape` reads at that pose:
**≤ 0.020 m without PHYS, 0.032 m with it.**

The ablation, on THIS tree with one file changed and nothing rebuilt (a `.tres` is not imported):

| `tin.tres` | `Run-StockTest` |
|---|---|
| shipped: `friction = 0.25`, `bounce = 0.1` | **0/3** |
| `friction = 1.0`, `bounce = 0.0` (engine defaults back) | **2/2 PASS** |
| `bounce = 0.0` only (friction 0.25 kept) | **PASS** (1/1) |
| `friction = 1.0` only (bounce 0.1 kept) | **PASS** (1/1) |

The single-variable rows say the two shipped values only fail TOGETHER: either engine default
alone reads under tolerance. Across the four rows the audited pose differs by under a degree of
tilt and 5 mm of height (from the bots' own samples: shipped `q=(-0.003,-0.005,-0.006)` at
`y=0.478`; bounce-only-off `(0.010,-0.005,0.003)` at `0.476`; friction-only-off
`(-0.002,-0.001,0.000)` at `0.479`; base `(-0.001,-0.001,0.000)` at `0.478`), and the depth
`CollideShape` reports for a cylinder against a box moves from under 0.020 m to 0.032 m across
that range. There is no monotonic relation between tilt and the reading — the bounce-off row is
the MOST tilted and passes — so the honest statement is: a can's rest pose in a hole is decided
by two material numbers at once, and the cylinder-vs-box depth query is not smooth in that pose.

**What this is NOT:** PROBE-1's sleep change, the settle latch's spin gate, or `CanSleep` — those
ship in the same lineage and the ablation leaves every one of them in place. And it is not the
suite: its own message already calls this shape "a landing penetration, i.e. a staging artefact",
and STOCK-1's handoff §(measured) recorded a 0.035 m board penetration from a 5 cm drop with the
same words. **The audit is doing its job** (the can is pushed 3.7 cm out of the board and rests
clean); the packet's bar of zero corrections on a DROPPED prop is the part that a physics material
with any bounce at all cannot hold.

**Done on this branch: `tin.tres` bounce 0.10 → 0.00**, with the measurement in the file's own
comment, and `PropMaterialTableTests.OnlyTinBounces` → `NothingBounces`. The friction that makes
cans roll (0.25, the row of the table Talon named out loud) is untouched; the ping was PHYS-1's
cosmetic call and nobody has ridden it. `Run-StockTest` after the change: {STOCK_AFTER}

---

## 4. The two positive controls (PHYS-1 §11.4)

Both planted, both went red where PHYS-1's table says they should, both reverted (the tree is
clean of them; `git status` on `scripts/` shows only the engine's `.uid` sidecars).

| control | the plant | what the suite / log said |
|---|---|---|
| **A — the contact wake never fires** | `return;` ahead of `_registry.Wake` in `PropManager.ServerBumpProp` | `Run-PhysicsFeelTest` **FAIL**: `holder: not one of the five boxes tilted past 60 deg -- a resting prop is still an immovable wall`, the same on the witness, and `the server logged not one [phys] wake for the whole run -- P1's contact path never ran, so every green bar above is about something else`. `PHYS1BARS ... wakes=0 restores=1`. The shove itself fired (`shoveMps=2.80`), the head moved, nothing behind it did |
| **B — CCD off, 20 m/s straight down into a 5 cm board** | `ContinuousCd = false` in `Carryable._Ready`; a bare server with the Stock hole's can seeded and `--phys-shove "1,0,-20,0,5"` | **CCD on:** the can is at `y=0.450` after the shove — still on `Aisle0_Bay0`, rim-tilted — and one `[phys] clamp` line (`12.00 total`: `MaxPropFallSpeedMps` clipped the 20 to 12 on the way in). **CCD off:** `y=-0.084` one sample later — through the board and 8 cm into the floor — five clamp lines, and it comes to rest ON THE FLOOR at `y=0.025`. Containment is CCD's; the per-tick clamp is not a substitute for it, exactly as PHYS-1 §2 says |

PHYS-1's note that "the packet's 20 m/s throw is unreachable by construction" was right about a
throw (`ThrowSpeedCapMps` 8.5) and wrong about the control: `--phys-shove` reaches
`ServerNudgeLoose`, which is not a throw, and the 12 m/s that survives the fall cap is still
0.2 m per tick against a 5 cm board. First attempt at A passed `-MutexTimeoutMinutes` to a suite
that has no such parameter and reported a red that was a usage error; read `suite.err.txt` before
believing an exit code.

**Control A was measured on the 2 cm fixture, before §1b**, so its domino half was red for the
fixture's reason as much as the plant's; the line that carries it is `not one [phys] wake` and
`wakes=0`, which the plant produces on any fixture and the shipped tree never does (`wakes=1..5`
on every run in this document). It is owed a re-run on the edge-on row and is listed in §9.

---

## 5. Server physics at rest, on the tip

PHYS-1 §11.1 measured `p95 2.550 ms, wakes=0` at `5da201c`. PHYS-2's three commits touch no
per-tick path — `--phys-shove` fires once per entry off the same clock as the seeded drop — but a
number that is not re-taken is a number quoted from a handoff forever:

`--reach-cost 35 --cost-shove-every 0`, `--world supermarket --spawn-room search`, 162 props,
**nobody connected**, two runs a side on the same afternoon (the base is `1511ea8`, FEEL-1 +
STOCK-1 without PHYS, so the level is the same):

| tree | run | p50 | **p95** | peak | `[phys] wake` |
|---|---|---|---|---|---|
| PHYS-2 `d8f024b` | 1 | 1.384 ms | **2.450 ms** | 22.020 ms | 0 |
| PHYS-2 `d8f024b` | 2 | 1.351 ms | **2.512 ms** | 21.968 ms | 0 |
| base `1511ea8` | 1 | 1.644 ms | **2.852 ms** | 23.536 ms | 0 |
| base `1511ea8` | 2 | 1.577 ms | **2.675 ms** | 22.896 ms | 0 |

PHYS-1's `2.550 ms` at `5da201c` holds; PHYS-2 added nothing at rest. `Measure-ShopFloor -Pass
rest` (two walking bots, the harness SHELF-1 wrote) read p95 `8.388` / `7.902 ms` on the tip —
that number has two bots waking a crate in it (`[phys] wake prop=1014` once per run) and is not
the at-rest figure; quoted so nobody compares it to the table above.

---

## 6. Captures

{CAPTURES}

---

## 7. Gates

- **xUnit:** `Failed: 0, Passed: 1944, Skipped: 0, Total: 1944` (PHYS-1's 1937 + 7 for
  `--phys-shove`'s parser and the angular triple).
- **`Run-PhysicsFeelTest` ×3 on the tip:** {FEEL}
- **Scene marathon (`Run-AllTests.ps1`) on the tip:** {MARATHON}

---

## 8. Deviations, stated

1. **`--phys-shove` grew an angular triple** the packet did not ask for. The reason is §1's third
   commit: without it the rolling-can bar is unstageable by construction.
1b. **The island wake (`ServerWakeIsland`) is a mechanism P1 did not name.** It is the part of "a
   resting prop is not an immovable wall" that a contact-time wake cannot do, measured three
   ways in §1b, and it is bounded (0.15 m, 16 props, one audit each, nothing at rest). If Talon
   would rather Resting props on the server SLEEP than freeze — which is the engine's own answer
   to this — that is a redesign of the settle latch and the wire's Resting, and this is the
   smallest thing that works until then.
1c. **The domino fixture changed twice**: pitch 0.21 → 0.30 (a lean that stalls) → edge-on at
   0.10 (dominoes). Each step is a measurement in the suite's header. PHYS-1's 2 cm row could
   never have fallen from one strike; its 5/5 was two bots walking through it.
1d. **`--seed-test-props` grew two optional fields** (roll about X, yaw about Y), drop-on-malformed
   like the rest of the flag; no existing caller changes.
1e. **The bounds audit's extents are exact per shape now** (`PlacementIntegrity.ExtentsInside`).
   That is REACH-1's code and it is a behaviour change for every round prop in the game: a can or
   an orange that rolled 45° is no longer teleported home for lying on the floor. Measured on
   the suite's can and on authored produce 1134/1135.
2. **Bar (1)'s window is read off the props, not the bot's clock.** The server's shove clock and a
   bot's own clock are separated by the connect delay and nothing in these logs correlates them.
3. **The base A/B for the two INT-era reds is confounded by STOCK-1's level** — `e75e04d` predates
   FEEL-1's merge onto this branch and no commit here has one without the other. Moot for
   `Carry: hold` (the FEEL-1 tip reproduced it) and for `Stock` (the synthetic `1511ea8` has both
   and passes 3/3).
4. **One self-inflicted stall, 15 minutes.** Running `Run-StockTest` through my own lane wrapper —
   which takes the suite mutex, as the suite then does again inside — deadlocked on itself and
   held the machine from STALL-1 and my own base runs until I killed my two PowerShell PIDs (no
   Godot involved). Written into the rules file: **a wrapper that takes the mutex may only wrap a
   suite that does not.**

---

## 9. What is owed

- **The spawn-pin fix** (§2.1) — to whoever owns `Gameplay.ApplySpawnPins`; not a physics change.
- **`Carry: hold` at t≈12 s** (§2) — FEEL-1's; the coordinate is in the rules entry.
- **A ruling on tin's bounce** (§3) — Talon's: the ping is gone (§3). If he wants it back, the Stock bar is what has to move — its own text says a DROPPED prop cannot hold "0 corrections".
- **Control A on the edge-on row** (§4): one plant, one run, after the marathon.
- **The bar for touching boxes.** Boxes side by side on a shelf are a book stack and slide as a
  block (§1b); that is honest physics and it is not "fall over like dominoes". Whether a shelf of
  cereal boxes should be seeded edge-on, spaced, or left as a slab that shoves off the board is
  Talon's, and the suite now measures whichever he picks.
- **Talon's ride.** The shove speeds (2.8 m/s row, 1 m/s can), the roll distance and the row's
  geometry are all in `Run-PhysicsFeelTest.ps1`'s header constants and named `.tres`/`.tscn` lines.

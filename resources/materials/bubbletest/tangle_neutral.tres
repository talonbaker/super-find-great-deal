; TANGLE NEUTRAL -- rung 2 of the section ladder. The tangle palette's header, and the reason
; hue 300 was picked, live in `tangle_ground.tres`; the ladder itself lives in `red_ground.tres`.
;
; This is the bulk of the pile: of 107 blocks the mid tone wears this one. BYTE-IDENTICAL to the
; other six `<section>_neutral.tres` and to `neutral_gray.tres` -- it is not re-derived, because
; the whole point of that file's history is that this level has ONE neutral and it does not
; drift.
; RUNG 2. Measured L* = 24.39.
[gd_resource type="StandardMaterial3D" format=3]

[resource]
albedo_color = Color(0.227, 0.227, 0.23, 1)
roughness = 0.88
metallic = 0.0
metallic_specular = 0.35

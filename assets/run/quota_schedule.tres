[gd_resource type="Resource" load_steps=2 format=3]

; The quota schedule (core-spine spec §2.2) — THE file a balance pass edits. Every number
; below is a playtest placeholder (spec §8); a tweak here changes the game's demand curve
; with zero code changes (CORE-PROG-A1 acceptance criterion 6, proven by --flow-selftest's
; schedule-tweak check). Field semantics and extremes: scripts/game/run/QuotaSchedule.cs
; and QuotaMath.cs (strict-increase floor, factor <= 1 degrades to +1/round, 1e9 clamp).

[ext_resource type="Script" path="res://scripts/game/run/QuotaSchedule.cs" id="1"]

[resource]
script = ExtResource("1")
EarlyRoundsDemand = PackedInt32Array(3, 5, 8, 12)
TailGrowthFactor = 1.35
CarrySurplus = true
